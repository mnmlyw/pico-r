# Conformance ledger

Gaps found by diffing pico-r's behavior against the official PICO-8 binary
(see `tools/oracle.sh`). Each fixed/confirmed gap has a probe cart in
`probes/` and a golden capture in `golden/`; `cargo test --test conformance`
keeps them from regressing.

## Fixed

| id | subsystem | root cause | fix location |
|---|---|---|---|
| print-shorthand gluing | preprocessor | `?` (print shorthand) was only recognized at true line-start and copied its argument text raw, bypassing every other transform (string-escape repair, bitwise ops, etc). Rewritten as part of the main per-character transform loop so it fires anywhere a bare `?` appears and its argument text gets fully transformed. Confirmed against the oracle that `?`'s args run to the **end of the physical line** unconditionally (comments excepted) — a trailing `end`/`;`/keyword on the same line is swallowed and fails to compile in official PICO-8 too, not a pico-r gap. | `src/preprocessor.rs` (`process_line`) |
| P8SCII control-code string escapes | preprocessor / lexer | `\^`, `\#`, `\-`, `\|`, `\+` inside string literals were falling through the lexer's escape table (`invalid escape` lex error). Verified via oracle probe (`ord(sub(s,i,i))`) that each collapses to exactly one control byte (0x06/0x02/0x03/0x04/0x05) with the following character passed through untouched — the existing `gfx.rs` P8SCII renderer already implements those control bytes, so this was purely a compile-time encoding gap. | `src/preprocessor.rs` (`p8scii_control_escape`) |
| `extract_lhs` string-call sugar | preprocessor | `\` (int-div) and other in-place operators use `extract_lhs` to find their left operand by scanning already-emitted output backward. It didn't recognize Lua's paren-less string-call sugar (`rnd"32"`), so `rnd"32"\1` failed to transform. | `src/preprocessor.rs` (`extract_lhs`) |
| `extract_lhs` unbalanced subscript | preprocessor | Same helper treated bare `[`/`]` as "keep extending" without depth tracking, so `tq[o\64+1]` incorrectly walked back through the *open* `[` into the array name (`tq[o` instead of `o`), corrupting the subscript. Now depth-tracked like `(`/`)`. | `src/preprocessor.rs` (`extract_lhs`) |
| `?` swallowed by expression-boundary scanners | preprocessor | `extract_rhs`/`extract_simple_expr`/`extract_bitwise_rhs` (used by compound-assign and bitwise-op transforms) didn't know `?` always starts a new statement, so e.g. `sum+=0 ?"x"` had the `?"x"` clause folded into the compound-assign's RHS. Added `?` as a hard stop, matching `;`. | `src/preprocessor.rs` (three `extract_*` helpers) |
| `printh` was a no-op | api | Needed for this whole harness to work at all — now writes to stdout on native builds via a shared `display_string` helper (also used by `tostr`). | `src/pico_lua/api.rs` |
| Division by zero returns `inf`/`nan` | interpreter | PICO-8's 16.16 fixed point has no infinity/NaN: `x/0` saturates to `±0x7fff.ffff` (confirmed: `1/0 == 0x7fff.ffff`), sign taken from the dividend; `0/0` saturates positive. `x%0` is always `0` (not derived from the saturated division). Since `\` is preprocessed to `flr(lhs/(rhs))`, this single fix also corrects `5\0` (`->32767`) and `-5\0` (`->-32768`). | `src/pico_lua/interp.rs` (`apply_binop`) |
| Default number-to-string precision | interpreter | PICO-8 displays non-integer numbers rounded to 4 decimal digits with trailing zeros stripped (`1/3 -> "0.3333"`, `2.99998 -> "3"`), not Rust's shortest-round-trip float formatting (which was printing values like `0.3333333333333333`). | `src/pico_lua/value.rs` (`number_to_str`) |
| `tostr()` format flags ignored | api | The second argument to `tostr` was never read. Confirmed exact semantics via oracle: `0x1` = hex `"0xHHHH.LLLL"`, `0x2` = raw 32-bit fixed value as a plain decimal integer, `0x3` = hex digits with no `.` separator. Also fixed `tostr()` with **no** arguments returning `"[nil]"` — confirmed official PICO-8 returns `""` for true no-args, distinct from the explicit `tostr(nil)` case. | `src/pico_lua/api.rs` (`api_tostr`) |
| `extract_lhs` dropped a leading unary minus | preprocessor | `-5\2` rewrote as `-flr(5/(2))` (`-2`) instead of `flr(-5/(2))` (`-3`) — the sign ended up outside the rewritten call instead of inside the divided operand, since `extract_lhs`'s backward scan stopped at `-` unconditionally. Now distinguishes leading unary minus (include it) from binary subtraction (`a-b\c`, correctly stop — the `\`'s lhs is just `b`) using the existing `is_prev_value` helper. | `src/preprocessor.rs` (`extract_lhs`) |
| `chr()` dropped out-of-range args instead of wrapping | api | `chr(256,300,-1)` produced a 0-length string (all three args individually failed a `0..=255` range check and were silently skipped). Confirmed against official PICO-8 that out-of-range ordinals wrap via `% 256` (`chr(256,300,-1)` -> 3-byte string `[0,44,255]`). | `src/pico_lua/api.rs` (`api_chr`) |
| Concat error message text | interpreter | `"a"..nil` raised `"concat nil"`, inconsistent with the "attempt to X a Y value" phrasing used by every other runtime error in the file and not matching official PICO-8's exact text (`"attempt to concatenate a nil value"`). | `src/pico_lua/interp.rs` (`apply_binop`, `Concat` arm) |
| `tonum()` format flags ignored | api | The second argument to `tonum` was never read. Confirmed exact semantics via oracle: `0x1` = hex parse (inverse of `tostr`'s hex format, optional `0x` prefix, optional `.` fractional part), `0x2` = raw fixed-bits reinterpretation (a string is parsed as a plain integer bit pattern; a number uses its own `to_fixed()` representation, which naturally wraps for out-of-range values), `0x4` = return `0` instead of `nil` on parse failure. | `src/pico_lua/api.rs` (`api_tonum`) |
| `pcall`/`error`/string-table removed | api / interpreter | Confirmed (binary + manual) these don't exist in official PICO-8 at all — pico-r had added all three as conveniences. See "Phantom features" below for the full writeup. | `src/pico_lua/api.rs`, `src/pico_lua/interp.rs` |
| `palt(bits)` bit order reversed | api | The single-bitmask form maps bit `k` directly to color `k`. Confirmed against official PICO-8 that bit `(15-k)` controls color `k` (bit 15 = color 0, bit 0 = color 15). | `src/pico_lua/api.rs` (`api_palt`) |
| `pal(_,_,2)` corrupted the draw palette | api | Both the table form (`pal(tbl,2)`) and scalar form (`pal(c0,c1,2)`) only special-cased `p==1` (screen palette); any other `p` including `2` (secondary/pen palette) fell into the draw-palette branch. Confirmed official PICO-8 leaves `0x5f00` untouched for `p==2`; made it a no-op there rather than fabricate the actual secondary-palette storage/effect (not yet implemented — see deferred list). | `src/pico_lua/api.rs` (`api_pal`) |
| `pal({[k]=v})` didn't wrap out-of-range keys | api | Iterated only keys `0..16`; a table with a key outside that range (e.g. `{[20]=5}`) was silently dropped instead of remapping via `key % 16`. Confirmed against official PICO-8. Now enumerates the table's actual entries (via its `HashMap<Key,Value>`) instead of assuming keys 0-15. | `src/pico_lua/api.rs` (`api_pal`) |
| `line()` endpoint wasn't memory-mapped | api / memory | The last `line()` endpoint (used for implicit line-continuation) lived only in Rust struct fields (`PicoState::line_x/line_y`), never written to RAM. Confirmed official PICO-8 exposes it at `0x5f3c`/`0x5f3e` (16-bit each) via `peek2`. Added `ADDR_LINE_X`/`ADDR_LINE_Y` constants and write them alongside the existing struct fields. | `src/pico_lua/api.rs` (`api_line`), `src/memory.rs` |

Corpus impact (188 real BBS carts, 120 frames each, `tests/conformance/sweep_baseline.tsv`):
**91→100 clean, 79→70 load errors, zero regressions** (preprocessor batch). The numbers/math and strings/P8SCII fixes are correctness fixes, not load-error fixes, so they don't move this count further; verified no *real* regressions on top of that baseline (`novaescape-0.p8.png` incidentally went `TIMEOUT -> CLEAN`). One apparent regression, investigated and ruled a test-harness artifact, not a real one: `onek_landscape06-0.p8.png` went `LOAD_ERROR -> TIMEOUT` after the `chr()` fix, because the cart's top-level code has `while(not btn(4)) do ... end` (waiting for the X button) at line 92, previously unreachable because a `chr()` call earlier in the same file crashed first. `run-cart`'s test harness only simulates the O button, not X, so it now hangs on that wait loop — confirmed official PICO-8 itself does **not** hang on this cart (`pico8 -x` exits cleanly). The underlying `chr()` fix is correct and oracle-verified; this is a gap in the sweep harness's input simulation, not in pico-r.

## Deferred / newly discovered (not fixed this batch)

- **`try_bitwise_op`'s RHS splice doesn't recurse.** Unlike compound-assign's RHS (which recursively calls `preprocess()` on its captured span), `try_bitwise_op` copies its captured RHS verbatim from the untransformed source. A backslash nested inside a bitwise RHS (e.g. `e&tq[o\64+1]`, from `picodex_dual-1.p8.png`) still isn't transformed. This is a broader "extracted spans don't recurse" pattern that likely also affects `try_peek_shortcut` — worth its own investigation rather than a one-off patch.
- **`_set_fps` global function missing.** Confirmed via oracle to be a real official PICO-8 API (`pico1kmatch-0.p8.png` calls it at the top level); pico-r has no equivalent.
- **`print()` pixel-level rendering diverges from official PICO-8 even for the trivial case** (`cls(0); print("a")` produces a different screen-byte hash). This is unrelated to the preprocessor work in this batch — likely a default text color, cursor position, or glyph-bitmap difference — and needs its own dedicated investigation before any print-shorthand test can safely assert on screen pixels. All probes in this batch use `printh`/computed values instead of screen hashes to avoid being confounded by it.
- Remaining `LOAD_ERROR` carts in the corpus (70) include a distinct compound-assign bug independently discovered: `obj.rem[axis] = obj.rem[axis] + (axis=="x" and) ox or oy` (`celesteprogrupter-2.p8.png`) — the RHS extraction for `+=` splits a parenthesized `and...or` ternary-style expression incorrectly. Not investigated further this batch.

### numbers/math section audit (architectural, deferred)

- **No 16.16 fixed-point quantization/wraparound in regular arithmetic.** `+`, `-`, `*` (and `/` outside the zero case just fixed) operate as plain `f64` math with no truncation to the 32-bit fixed grid and no overflow wraparound. Confirmed: `32767.5+1` wraps to `-32767.5` on official PICO-8; pico-r gives `32768.5`. `20000*2` similarly wraps (`-25536` on official). This is the single biggest remaining numeric gap — fixing it properly means representing every `Value::Number` as a quantized-after-every-op fixed value (or switching the representation to a raw `i32` entirely), which is a core-VM change, not a local fix. The `to_fixed`/`from_fixed` helpers in `api.rs` (already used by the bitwise ops) are the natural quantization primitives to reuse when this gets tackled.
- **`abs(-32768)` already matches** (`32768` both sides) — the numbers/math audit agent guessed this was broken (predicting `-32768` from wraparound); verified it is NOT a gap. Recorded here so it isn't re-investigated.
- **`sin`/`cos`/`atan2` bypass their own LUTs.** `src/trigtables.rs` defines PICO-8-accurate `SINTABLE`/`ATANTABLE` but nothing calls them — `api_sin`/`api_cos`/`api_atan2` use `std::f64::sin/cos/atan2` directly (after correctly converting PICO-8's "turns" input and flipping sign for the y-down convention, which the audit agent had *also* guessed was broken but isn't). Real gap is precision only: pico-r's trig doesn't reproduce the 4096-entry LUT's quantization steps, so nearby inputs that should land on the same table bucket (and thus produce an identical value on real hardware) instead vary smoothly. Low real-world impact; not fixed this batch.
- **`tostr`'s 4-decimal display doesn't handle exact-tie precision** the same way official PICO-8 does: `tostr(0.12345)` -> pico-r `"0.1235"`, official `"0.1234"`. Same root cause as the fixed-point-quantization gap above — official PICO-8 rounds the *already-fixed-point-quantized* value, and without that quantization step a handful of high-precision inputs round differently at the last digit. Rare in practice (most real carts display values with far less precision); will resolve automatically once the fixed-point epic above is tackled.

### strings/P8SCII section audit

- **`split()` with a multi-character separator string is not literal-substring matching** — confirmed via oracle that `split("a-b_c","-_")` returns `{"a","b_c"}` (2 elements, split only on `-`), not `{"a-b_c"}` (no split, since the literal substring `"-_"` never appears) as pico-r's current `api_split` would produce. The exact rule (charset-style per-character matching? first-character-only?) wasn't pinned down further this batch — needs one more oracle round before fixing. Not yet fixed.
- **`Value::as_number` string coercion requires the whole byte string to be valid UTF-8** (`value.rs`) — a numeric-looking string with an embedded P8SCII byte ≥0x80 (e.g. `"3"..chr(200)`) fails to coerce entirely via `str::from_utf8`, even though only the trailing byte is non-numeric. Not confirmed against the oracle this batch (needs a probe); flagged from code reading only.

#### Phantom features: pico-r had functions/methods that don't exist in official PICO-8 at all (removed)

Not "gaps" in the usual sense (missing behavior) — the reverse: pico-r was accepting code that would **crash on real hardware**. Confirmed two ways: (1) via oracle (`tostr(pcall)`, `tostr(error)`, `tostr(string)` are all `[nil]`/errors across cart-version headers 8/16/27/32/41/42 on the actual installed binary, v0.2.7 per `Info.plist`, ruling out a version-gating explanation), and (2) cross-referenced against the official manual (fetched independently): no mention of `pcall`, no `error()` function, no `string` library — only standalone functions like `sub()`/`chr()`/`ord()`, never `string.sub()`. Removed:

- **`pcall`/`error` globals** — deleted `lua_pcall`/`lua_error` and their registrations entirely (`api.rs`).
- **The `string` table and colon-method dispatch on strings** — deleted the `string` table registration and its `str_len`/`str_lower`/`str_upper`/`str_rep`/`str_format` helpers, and removed `table_get`'s (`interp.rs`) fallback that routed `s:method()` calls to it. Also tightened `MethodCall`'s error to say `"attempt to call method 'X' (a Y value)"` — matching official's exact phrasing — instead of the generic `call_value` message, for any non-callable method lookup (not just strings).
- **NOT a phantom feature** (verified, ruling out a guess, kept as-is): bracket-indexing a string (`("hello")[1]`) returns `"h"` on **both** official PICO-8 and pico-r — this is real, matching behavior.

Verified zero regressions on the 188-cart corpus after removal (confirming no real cart relied on these) beyond the already-documented `onek_landscape06-0.p8.png` harness artifact above.

### gfx draw-state section audit

- **`fillp()` DOES apply to `oval()`/`ovalfill()`** — a wiki claim (that the fill pattern list excludes ovals) was checked against the oracle and refuted: fillp'd and non-fillp'd ovalfill hashes differ on official PICO-8 just as they do without fillp being oval-specific. Recorded so it isn't re-investigated as a gap.
- **`ovalfill()`'s baseline (no-fillp) screen hash doesn't match official PICO-8 either** — found while checking the fillp claim above. This is a genuine, separate rasterization-precision gap (not a draw-state issue), handed off to the upcoming gfx-rasterization section (task #12) rather than investigated here.
- **`circfill()`'s invert fill triggers from the wrong condition.** `poke(0x5f34,0x2)` alone has zero effect on official PICO-8 (confirmed: hash identical to a non-inverted circfill) — invert apparently also requires specific bits (reportedly `0x1800`, per forum text, not yet oracle-verified) set in the *per-call color argument*, which pico-r doesn't look at at all (it inverts from the `0x5f34` register alone, and pico-r's hash *does* differ from its own non-inverted baseline — a real, confirmed-broken trigger condition). Fixing this needs the exact color-argument bit-encoding pinned down first; not done this batch.
- **`pal(_,_,2)`'s actual secondary-palette storage/effect is still unimplemented** — only fixed the confirmed part (it must not touch `0x5f00`). Where the "pen"/secondary palette actually lives in memory and how it's consumed by fillp-based dithering wasn't investigated further; currently a safe no-op rather than a full implementation.
- **`print()`'s cursor-x register (`0x5f26`) is never written back after printing, and the `\n` reset value should come from `0x5f24`** (a documented "margin" register), not the original call's `x`. Found while probing draw-state registers, but this is really a print/font-subsystem concern — deferred to that section (task #13) rather than partially fixed here.

### gfx rasterization section audit

- **`draw_oval` had a real, high-impact algorithm bug — fixed.** The old single-loop incremental midpoint-ellipse implementation could terminate before its stepping variable reached zero, entirely missing the top/bottom pole pixels — confirmed even for an exact-integer bounding box (not just an odd one). Replaced with a per-row (and, for outlines, also per-column) exact scan directly from the ellipse equation, which can't have that class of bug. Two conventions had to be reverse-engineered empirically (row-by-row/pixel-by-pixel diffing against the oracle, not guessed): (1) `x0..=x1`/`y0..=y1` are inclusive pixel ranges, so the *continuous* radius is `(x1-x0+1)/2` — using `(x1-x0)/2` shifts the whole curve and breaks top/bottom symmetry; (2) sampling must use pixel *centers* (`p+0.5`) on both axes. `ovalfill` now matches official pixel-for-pixel for every size tested except one (below); `oval` (outline) needed a **second**, column-based scan for the near-pole regions, where the curve is nearly horizontal and official fills a short flat run of pixels rather than just the two boundary columns — matches exactly for every size tested.
- **One residual, unresolved `ovalfill` mismatch**: for a 80x60 bounding box specifically, 4 of 61 rows (symmetric pairs, e.g. row 12/68 and 18/62) are off by exactly 1px on each side. Confirmed NOT a floating-point precision artifact (`f32` gives the identical result to `f64` at these rows). Most likely official's real rasterizer is itself an incremental algorithm with its own discretization quirks at specific boundary cases, rather than the exact-continuous-math this fix uses — replicating it bit-for-bit would mean reverse-engineering an undocumented, closed-source algorithm further, which weighed against the value of the last few pixels wasn't done this batch. 57/61 rows match exactly (this was previously ~0/61 correct beyond luck).
- **`circ()`/`circfill()`/`line()` (Bresenham) are already pixel-exact** — verified via full hash + spot-pixel checks (a community forum claim of a small-radius "rightward skew" in native `circ()` was checked and refuted for the radii tested). No work needed; recorded so these aren't re-investigated.
- **`sspr()`'s scaling has a confirmed, unresolved rounding mismatch.** Verified via a full pixel-grid diff (not just a hash) that pico-r's `px*sw/dw` nearest-neighbor source-index formula produces a slightly different (shifted-by-one-ish) sampling pattern than official for non-integer scale ratios (tested 8x8 -> 25x25). Tried the analogous "pixel-center" fix that resolved the oval case (`(px+0.5)*sw/dw`, and a "map destination center back to source" variant) — neither matched official exactly. Needs more oracle samples to pin down the exact formula; not fixed this batch.

Corpus regression sweep: zero new regressions (same already-documented `onek_landscape06-0.p8.png` harness artifact).

### print/font section audit — resolved the session's original `print()` mystery

- **`src/gfx_font.rs`'s entire font table was wrong, and this was the root cause of the very first divergence noticed in this whole audit** (`cls(0);print("a")` producing a different screen hash than official PICO-8, flagged as unexplained in the first commit's ledger entry). Root cause: pico-r's font hard-coded **the same bitmap for uppercase and lowercase letters** (`data[(letter+32)] = data[letter]`), but official PICO-8 renders uppercase A-Z as a distinct "small caps" glyph set (row 0 always blank, glyph compressed into rows 1-5) — a real, different bitmap, not lowercase shifted. Confirmed by bulk-dumping all 224 printable P8SCII glyphs (32-255) directly from the official binary via a single oracle probe (`tests/conformance/probes/font_glyphs.p8` regression-locks a representative sample) and mechanically regenerating `FONT_DATA` from that dump — **196 of 224 glyphs were wrong**, not just the upper/lower case ones. `cls(0);print("a")`'s screen hash now matches official exactly (`23296`), as does a full mixed-case/digit/symbol string and the two full alphabets. Zero regressions on the corpus despite this touching every text-rendering cart.
- **`print()`'s return value only reflected the last line's x, not the max across all lines.** Confirmed against official: `print("hello\nb")` returns the x reached after `"hello"` (the longer first line), not after `"b"`. `draw_text` now tracks a running max. | `src/gfx.rs` (`draw_text`)
- **`print()` never persisted the cursor registers when explicit x/y/col were given, and even the two argument forms that did update them only advanced `cursor_y` by a flat `+6` regardless of embedded newlines.** Confirmed against official: cursor_x always resets to *this call's* starting x (not wherever printing ended up, and not the previous register value) and cursor_y advances by `6 * (1 + newline count)`. All three `print()` argument forms now persist both registers uniformly. | `src/pico_lua/api.rs` (`api_print`)
- **The `0x5f24` "margin" register (deferred from the gfx draw-state section) is now precisely characterized as a separate, narrower, still-open gap**: `cursor(x,y)` and an internal `\n` normally reset to the same x, so the fix above already covers ordinary usage — but a cart that explicitly `poke(0x5f24, n)`s a *different* value still diverges (confirmed: `cursor(20,20); poke(0x5f24,60); print("a\nb")` -> official's cursor_x ends at `60`, pico-r's at `20`). Fixing this fully means threading a real persistent margin register through `draw_text`'s `\n`/reset handling instead of using the call's literal `start_x`; not done this batch given how rarely a cart would diverge the two on purpose.
- **Found but not oracle-verified this batch** (code-reading only, from `gfx.rs`'s control-code handling): control code `0x02` takes 3 parameters (offset-x, offset-y, char) where the manual describes a 1-parameter "solid background colour" code; `0x03`/`0x04`/`0x05` treat their parameter byte as signed (`i8`, -128..127) where the manual's `P0-16` framing implies a different bias/range; `0x0E`/`0x0F` are implemented as char-width toggles but the manual describes them as custom-font-switch codes; control code `0x0B` (`\v`, "decorate previous character") has no handler at all. Also: auto-scroll when printing past the bottom of the screen appears entirely unimplemented (`grep -rn scroll src/` is empty), and the custom font at memory `0x5600` isn't wired up (`draw_char` always reads the static table, never memory) — both real feature gaps, not yet oracle-verified for exact behavior.

### map/sprites section audit

- **The big-map width register (`0x5f57`) had its zero-case backwards — fixed.** pico-r treated a raw `0` in this register as width 128 (and never initialized the register at boot, so it defaulted to `0` and happened to hit that branch by coincidence). Confirmed against official PICO-8 that `0` actually means width **256** (`poke(0x5f57,0); mset(200,10,7); mget(200,10)` -> `7` on official, `0` on pico-r pre-fix). Since flipping the interpretation alone would have broken the normal 128-wide default, `Memory::new`/`init_draw_state` now explicitly write the byte `128` at boot instead of relying on the old (wrong) zero-special-case — verified the plain default-width case still behaves identically before and after. | `src/gfx.rs` (`map`/`map_get_wide`/`map_set_wide`), `src/memory.rs`
- **`fget`/`fset` out-of-range index handling already matches** — verified via oracle (`fget(-1)` and `fget(9999)` both return `0` on official PICO-8, same as pico-r's existing clamp). No fix needed; recorded so this isn't re-investigated.
- **Two candidates from this section's audit were empirically inconclusive, not confirmed either way** — flagging rather than guessing:
  - Whether `0x5f56` (the big-map base-address register) is meant to also remap the *normal* (`x<128`) map region, not just the wide-extension columns: a probe poking `0x5f56=0x10` then `mset(0,0,9)` showed **neither** `peek(0x1000)` nor `peek(0x2000)` becoming `9` on official PICO-8 — contradicting both pico-r's current behavior (always writes `0x2000`) and the hypothesis that it should write `0x1000`. The write evidently lands somewhere else this probe didn't check, or `0x10` isn't a value official interprets the way assumed. Needs a properly designed follow-up probe before touching this code.
  - The shared sprite/map memory aliasing (sprite rows 64-127 and map rows 32-63 sharing physical bytes at `0x1000-0x1FFF`): pico-r's `sset(0,64,5)` visibly affects `mget(0,32)` (reads back `5`), demonstrating pico-r's own storage *is* shared as designed — but the identical probe on official PICO-8 shows `mget(0,32)` staying `0`, meaning either the exact row/nibble-offset arithmetic assumed for this probe is wrong, or the real aliasing works differently than modeled. Not chased further this batch; needs a probe that empirically discovers the correct address formula (e.g. sweep `poke()` at known addresses and read back via both `sget`/`mget`) rather than assuming one.

Corpus regression sweep: zero new regressions (same already-documented `onek_landscape06-0.p8.png` harness artifact).

### memory/registers section audit

- **`cstore()` didn't protect the compiled-code region (`>=0x4300`) — fixed.** Confirmed against official PICO-8 that `cstore()` silently *skips* writes at or beyond `0x4300` rather than writing through or erroring — verified precisely with a destination range straddling the boundary (`cstore(0x42fe,0,4)`: the two bytes landing at `0x42fe`/`0x42ff` write through, the two at `0x4300`/`0x4301` are silently dropped). pico-r wrote through unconditionally. | `src/pico_lua/api.rs` (`api_cstore`)
- **Memory addresses didn't use `flr()` semantics for negative fractional values — fixed.** Confirmed `peek(-0.5)` reads the same byte as `peek(-1)` on official PICO-8 (both wrap to `0xffff`), not address `0` (which truncate-toward-zero, `as i32`, would give for `-0.5`). Added a dedicated `arg_addr` helper (floor before cast) and applied it to the address arguments of `peek`/`poke`/`peek2`/`poke2`/`peek4`/`poke4`/`memcpy`/`memset` — deliberately scoped to address parsing only, not the shared `arg_int`/`safe_to_i32` helpers used pervasively elsewhere for unrelated arguments that haven't been verified to need the same treatment. | `src/pico_lua/api.rs`
- **Two candidates were oracle-tested and refuted** (already matching official, no fix needed): `fget`/`fset` out-of-range indices already return `0` like official; `memcpy` with a negative length or a destination range extending past `0xffff` already no-ops/clamps like official (doesn't wrap and corrupt low addresses, and doesn't perform a huge accidental copy) — both had been flagged as *suspected* bugs from code-reading alone, refuted only once actually run against the oracle.
- **`stat(0)`/`stat(1)` (memory/CPU usage) are hardcoded constants**, not tracking real heap/frame-time usage — a known, pre-existing stub. Real carts occasionally branch on these for debug overlays; left as-is since replicating PICO-8's exact profiling isn't practical, and this is really `system/stat`-section territory (task #16) rather than a memory-access bug.
- **Tooling fix, not a pico-r conformance bug**: discovered that running a `cstore()`-using probe through `tools/oracle.sh` caused the *official binary itself* to rewrite the cart file back to disk on exit (version bump 42→43, ~64 lines of default `__sfx__` boilerplate appended) — official PICO-8 treats a cart that called `cstore()` as "dirty" and auto-saves it, even under `-x`. This was mutating the *committed probe file* in place, since oracle.sh ran the binary directly on it. Fixed `oracle.sh` to copy the probe into the scratch `-home` dir first and run the copy, so the committed source can never be touched by this side effect (verified: previously-committed probes were all unaffected since none of them exercised `cstore()` before this section).

### system/stat section audit

- **`stat(16..19)`/`stat(20..23)` were swapped — fixed.** Confirmed against official PICO-8: `stat(16..19)` is the per-channel *sfx index*, `stat(20..23)` is the *note index* — pico-r's two branches had these reversed (verified with `sfx(3,0); stat(16)` -> `3` officially, was returning the note index instead). | `src/pico_lua/api.rs` (`api_stat`)
- **`stat(100)` (breadcrumb label) now returns `nil` when unset** instead of falling through to the generic `0` every other unhandled `stat()` code defaults to — confirmed via `type(stat(100))`. | `src/pico_lua/api.rs` (`api_stat`)
- **`cartdata()`/`dget`/`dset` don't actually persist across sessions — a real, confirmed, but larger gap, not fixed this batch.** Verified official PICO-8 durably persists cartdata: writing via `dset` in one process and reading via `dget` in a *separate* `pico8 -x` invocation (same `-home`, simulating a fresh cart load) correctly returns the persisted value. pico-r's `cartdata()` is a pure no-op and `dget`/`dset` only read/write RAM for the current run — the only persistence mechanism anywhere in the codebase is the manual, single global save-slot triggered by pressing P/L in the web player (`lib.rs`'s `web_save_state`/`web_load_state`, unrelated to `cartdata()` and not scoped per-cart-id). Unlike this batch's other fixes, implementing this properly means adding new WASM↔JS boundary exports so the browser can back per-cart-id `dset`/`dget` calls with `localStorage` automatically — a genuinely different scale of change than a local Rust bug fix, and the native `run-cart` CLI used for testing has no equivalent concept of "persistent storage" to build against without extra tooling. Flagged for a dedicated future pass rather than attempted here.
- **`menuitem()` and `extcmd()` are fully inert** (no pause-menu system exists to hook into; `extcmd`'s documented actions like `"screenshot"`/`"video"`/`"pause"`/`"reset"` are all no-ops) — real gaps, but not practically checkable via a headless `printh`-based probe (they're GUI/host-integration features), so not oracle-verified or fixed this batch.
- **`stat(4)` (clipboard contents) and `stat(31)` (last keyboard character) are hardcoded empty strings**, and `stat(6)` (launch parameter string) is likewise always empty — all three require live input/host-environment state this session's headless oracle probes can't simulate, so left unverified rather than guessed at.

Corpus regression sweep: zero new regressions (same already-documented `onek_landscape06-0.p8.png` harness artifact).

### audio API section audit

- **`sfx(id, ch, offset)` with an out-of-range offset didn't clamp — fixed.** Confirmed against official PICO-8 that an offset outside `0..32` clamps to the nearest valid note (`sfx(5,0,99)` starts at note `31`, the max), it doesn't fall back to note `0`. | `src/audio.rs` (`play_sfx`)
- **`music(pattern, fade, mask)` conflated an explicit `mask=0` with an omitted mask — fixed.** Both previously collapsed to the same "reserve all 4 channels" default (`if mask > 0 { mask } else { 0xF }`), but confirmed against official PICO-8 that an *explicit* `music(0,0,0)` reserves **zero** channels, leaving all 4 free for `sfx()` — verified by checking all four `stat(16-19)` channel states, not just one. Fixed by giving the "omitted" case its own sentinel (`-1`) distinct from an explicit `0`, so only a truly-omitted mask still defaults to all channels reserved. | `src/audio.rs` (`play_music`), `src/pico_lua/api.rs` (`api_music`)
- **`sfx(id, -2)`'s exact immediate-vs-release-loop stop semantics are confirmed to diverge, but deliberately NOT changed this batch — too risky without stronger evidence.** A probe checking `stat()` immediately after `sfx(5,-2)` shows official reports the channel as stopped right away, while pico-r's current `release_loop` only marks the loop as released (letting the current pass/tail finish, `finished` stays `false` until the audio naturally ends). The existing code has a comment explicitly citing this exact release-instead-of-stop behavior as a deliberate fix for a real cart (`mansion_bros`' vacuum SFX) — changing it based on a single immediately-after-the-call `stat()` snapshot risks regressing that already-fixed case if the real distinction is "stat reports stopped immediately, but audio synthesis still tails off," which this session's tooling can't distinguish from "actually stops instantly" without recording actual audio samples over time. Needs dedicated investigation (and probably a way to observe the audio engine's internal state over several frames, not just `stat()` once) before touching this.
- **The default channel-reservation behavior when `music()`'s mask argument is omitted entirely couldn't be conclusively tested.** A probe comparing `music(0)` (no mask) against explicit `music(0,0,15)` was ambiguous because this session's probe carts have no real `__music__` pattern data — with an empty/silent pattern, there's no way to distinguish "channel is reserved but produces no sound" from "channel was never actually reserved," so `sfx()` could freely use channel 0 in both cases regardless of which behavior is correct. pico-r's existing default (omitted mask reserves all 4 channels) was left unchanged since there's no counter-evidence, but it's genuinely unverified — a follow-up would need a probe cart with real per-channel music pattern data.
- **`sfx-auto-channel-ignores-music-reservation`** (the audit's top-priority candidate) also couldn't be conclusively tested for the same reason (no real music pattern data to observe genuine channel contention) — deferred alongside the item above rather than guessed at.

### Lua/preprocessor semantics section audit (final section)

- **`del()` during `all()`-iteration could skip or double-visit items — fixed.** The iterator only tracked "did the table's length shrink since last call" to decide whether to advance, which can't distinguish *where* a deletion happened: deleting a *later* item also shrinks the length but leaves the current position's content unchanged (must still advance normally), while deleting the *current* item shifts a later item down into this same slot (must re-visit it) — confirmed against official PICO-8 that the two cases genuinely differ (`del(objs,4)` from inside a loop currently at `v==2` must NOT cause `2` to be re-visited, but pico-r did). Fixed by tracking the last *value* returned (not just the index) and re-checking whether that position still holds it, rather than inferring from length alone. Verified this doesn't regress the already-working "delete the current item" and "delete every even item" patterns. **Confirmed real-world impact**: fixed a previously-crashing corpus cart (`shaiza-1.p8.png`: `UPDATE_ERROR -> CLEAN`) on top of the usual zero-regressions sweep. | `src/pico_lua/api.rs` (`api_all`)
- **Arithmetic and concat operators ignored table metatables entirely — fixed for `__add`/`__sub`/`__mul`/`__div`/`__mod`/`__pow`/`__concat`.** Confirmed against official PICO-8 (whose own manual has a worked example redefining `+` via `__add` for 2D vector math) that these ARE real, working metamethods — unlike `pcall`/`error`/the string library earlier this session, this is core language behavior PICO-8 does support. `apply_binop` previously called `as_number()`/`as_str()` directly with no metatable lookup, so e.g. `vector1 + vector2` crashed with `"arith on table"` instead of dispatching to `__add`. Fixed by checking each operand's metatable (operand `a` first, then `b`, matching standard Lua precedence) before falling back to the existing raw-value logic — verified `__add`/`__sub`/`__mul`/`__concat` all dispatch correctly and non-table operands are completely unaffected. | `src/pico_lua/interp.rs` (`apply_binop`, new `metamethod` helper)
- **Found but not fixed this batch, lower priority / more invasive** (each would need touching a different, more scattered call site than the contained `apply_binop` fix above): `__eq`/`__lt`/`__le` (table comparison) are ignored by `values_equal`/`compare`; `__newindex` is ignored by `table_set` (writes always land directly, no proxy/validation hook); `__len` is ignored by the `#` unary operator (always uses `raw_len()`); `__tostring` is ignored by `tostring()`/implicit string coercion (tables always print as `"[table]"`/an address-style placeholder). None of these were oracle-verified this batch (unlike `__add` etc., which were) — flagged from code-reading and standard Lua 5.2 semantics only, so treat as *plausible*, not confirmed, until checked against the oracle.
- **`pairs()`/`next()`'s key order is Rust `HashMap` order (randomized per process), not Lua's own unspecified-but-stable-per-build order.** A cart iterating a hash-keyed table with `pairs()` and relying on *consistent* (if arbitrary) ordering across runs — e.g. for seeding a deterministic-looking layout — could see different behavior between pico-r launches, and possibly between pico-r and official PICO-8's own (different but consistent) internal order. Real PICO-8's exact order isn't grounded anywhere accessible this session; flagged as a known source of divergence for hash-part iteration, not fixed.

Corpus regression sweep: zero new regressions (same already-documented `onek_landscape06-0.p8.png` harness artifact); two improvements, including the `shaiza-1.p8.png` fix noted above.

## Overall corpus impact across all ten sections

Starting from the harness-bootstrap baseline (91/188 clean), the ten section audits above collectively fixed real, oracle-confirmed gaps across the preprocessor, number formatting, string handling, three graphics subsystems, memory, system/stat, audio, and core Lua semantics — with **zero net regressions** at every checkpoint along the way (the one recurring "regression," `onek_landscape06-0.p8.png`, was investigated once and traced to a test-harness input-simulation limitation, not a real behavior change). A full re-sweep after all ten sections puts the corpus at **102/188 clean (up from 91/188)** — two carts were confirmed fixed outright by name (`novaescape-0.p8.png`, `shaiza-1.p8.png`), the rest of the net gain came from the preprocessor batch's load-error fixes. `tests/conformance/sweep_baseline.tsv` reflects this current state. The very first bug found (a broken font table) turned out to be the root cause of the divergence that motivated this whole audit in the first place. Every fix above has a standing regression probe in `tests/conformance/probes/` with a golden captured from the official binary, so `cargo test --test conformance` keeps all of this locked in going forward.

### Follow-up: compound-assign `and`/`or` splitting bug (post-audit)

After the ten sections above, went back to a bug flagged early in this session but not yet fixed: `extract_rhs` (used by compound-assign, e.g. `+=`) has a heuristic that stops capturing the right-hand side when it sees "a value, then a space, then what looks like the start of a new statement" — but it never checked whether the *word immediately before that space* was itself `and`/`or`/`not`. So a very common PICO-8 idiom, `x+=cond and a or b`, was splitting right after `and`, producing `x = x + (cond and) a or b` — leaving `a or b` dangling outside the parens as malformed code. Confirmed on a real corpus cart (`celesteprogrupter-2.p8.png`: `obj.rem[axis]+=axis=="x" and ox or oy`). Fixed by checking the preceding word against the same `is_operator_keyword` check already used for the *following* word; verified the original heuristic's actual purpose (detecting a genuine same-line statement boundary like `x+=1 y=2`) is unaffected. | `src/preprocessor.rs` (`extract_rhs`)

Corpus re-sweep: **103/188 clean** (up from 102), one full fix (`witchcrafttd-6.p8.png`: `LOAD_ERROR -> CLEAN`) and three carts progressing further (`to_take_root_among_the_stars_1-7.p8.png`, `tomorrow-6.p8.png`, `ultimate_selfie-2.p8.png`). Two carts show as "regressions" by the coarse rank metric (`celesteprogrupter-2.p8.png`/`-3.p8.png`: `LOAD_ERROR -> TIMEOUT`) but are **not real regressions** — investigated and confirmed this is the third occurrence this session of the same pattern (see the `chr()`/`onek_landscape06-0.p8.png` note above): removing the parse error that used to block these carts early now lets their top-level code reach `flip() goto _lp` — an old-style Celeste-derivative game loop that needs real coroutine/`flip()` support pico-r doesn't have (a known, pre-existing, documented limitation, not something this fix caused or could reasonably fix).

### Follow-up: clustering the remaining LOAD_ERROR carts (post-audit, round 2)

Went back to the ~63 still-failing `LOAD_ERROR` carts, clustered their exact error messages by normalized signature (same method as the very first workflow at the start of this whole session), and sampled the biggest clusters (`expected ')'`, `unexpected token in expression`, `expected 'then'`) for a common root cause. Found and fixed two more real, distinct preprocessor bugs:

- **A short-if condition that continues onto a second physical line via a dangling binary operator (e.g. `if(a>0)==\n(b>0) then`) was misexpanded as if the operator fragment were the entire if-body.** `is_continuation_body` (added earlier for the analogous `or`/`and` case) only recognized `or`/`and` as signs of a multi-line condition — a lone trailing operator like `==` wasn't covered, so `if(a>0)==` got wrongly turned into `if a>0 then == end`, swallowing the real condition/then that followed on the next line. Confirmed on a real corpus cart (`balloon-1.p8.png`: `if(btn(A) or btn(B))==` / `(btn(C) or btn(D)) then` spanning two lines). Fixed by extending `is_continuation_body` to also recognize a body that's *nothing but* a dangling operator (`==`,`~=`,`<=`,`>=`,`..`,`<`,`>`,`+`,`-`,`*`,`/`,`%`,`^`). | `src/preprocessor.rs` (`is_continuation_body`)
- **A short-if whose body is a `?` print-shorthand had its synthetic closing `end` swallowed.** `expand_short_ifs` runs *before* the pass that converts `?` to `print(...)`; when it wrapped a `?`-body by literally appending the text `" end"` after copying the body verbatim, that later `?`-conversion pass (which also consumes to end-of-line, per the earlier `?`-shorthand fix) would swallow the inserted `end` too — `if(msg~=nil)?msg,0,0,8` became `if msg~=nil then print(msg,0,0,8 end)` instead of `if msg~=nil then print(msg,0,0,8) end`. Confirmed on two real corpus carts (`pong_xmas-0.p8.png`, `lv-2.p8.png`). Fixed by having `expand_short_ifs` itself perform the `?`-to-`print(...)` conversion when it detects this specific body shape, so no raw `?` is left for the later pass to re-consume. | `src/preprocessor.rs` (`expand_short_ifs`)

Corpus re-sweep: **105/188 clean** (up from 103). Two more full fixes (`balloon-1.p8.png`, `beanosified123-4.p8.png`: `LOAD_ERROR -> CLEAN`); `pong_xmas-0.p8.png` progressed to a later, different parse error (genuine progress, same exit code so doesn't move the coarse count). One cart shows as a "regression" by the coarse metric (`lv-2.p8.png`: `LOAD_ERROR -> TIMEOUT`) but is the same already-documented, non-regression pattern as `celesteprogrupter-2/3.p8.png` above — confirmed its top-level code also reaches a `goto`-based `flip()` game loop pico-r's lack of coroutines can't run.

Sampled the two largest LOAD_ERROR clusters that remain (a `bitwise-and combined with a binary literal and a comparison`, e.g. `fget(s) & 0b11010011 > 0`, seen in at least 3 carts; and an `if(cond) do` block-opener PICO-8 apparently accepts as an alternative to `then`, confirmed real via oracle but seen in only 1 cart so far) — both are real, plausible gaps but weren't fixed this round given diminishing time budget; flagged here for a future pass rather than guessed at.

### Follow-up: `try_bitwise_op`'s RHS didn't recurse (post-audit, round 3)

Went back to the `bitwise-and + binary-literal + comparison` cluster identified above, and to a previously-deferred finding from the memory/registers section (`picodex_dual-1.p8.png`: a backslash int-div nested inside a bitwise RHS wasn't transformed). Both turned out to be the **same root cause**: `try_bitwise_op` captured its RHS from the raw, untransformed source and spliced it into the output *verbatim* — unlike `try_compound_assign`'s RHS, which already recursively calls `preprocess()` on its captured span. So any nested transform inside a bitwise expression's right-hand side (a binary literal like `0b11010011`, a backslash int-div, another bitwise op) reached the real Lua lexer completely untransformed and unparseable. Fixed by applying the exact same recursive-`preprocess()` pattern `try_compound_assign` already uses. | `src/preprocessor.rs` (`try_bitwise_op`)

Corpus re-sweep: **109/188 clean** (up from 105) — **zero regressions**, six improvements, four of them full clean fixes (`jerumabebi1-0.p8.png`, `spirit_solstice-9.p8.png`, `sujurejaba-0.p8.png`, `trichromat-0.p8.png`). The single best fix of this whole follow-up round. `picodex_dual-1.p8.png` progressed past its original error but hit yet another, apparently unrelated `&`-related issue further into the same file — not chased further given the size of the win already banked.

### Follow-up: pushing toward 100% clean — `?` glued directly onto a preceding keyword (round 4)

User set an explicit goal of reaching 100% corpus cleanliness. Reclassified all 79 remaining non-clean carts (54 `LOAD_ERROR`, 9 `TIMEOUT`, 7 `INIT_ERROR`, 6 `DRAW_ERROR`, 3 `UPDATE_ERROR`) and re-sampled the largest cluster (`LOAD_ERROR`, "expected 'then'"/"expected 'do'", 11 carts).

- **`?` (print-shorthand) glued directly onto the end of a preceding keyword with no separating space fused into a single bad identifier for the real Lua lexer — fixed.** Size-golfed carts write `if a>10then?"x"` with zero whitespace anywhere. The main `?`-to-`print(` conversion in `process_line` spliced in the literal text `print(` right after whatever preceded the `?`, with no regard for whether that produced two adjacent identifier characters — `then` + `print(` became the single token `thenprint(`, which the Lua lexer can't parse as `then` followed by a call. (The synthetic `?`-in-short-if path in `expand_short_ifs` already inserted a space after its synthesized keyword, so it wasn't affected — this was specifically the general, non-short-if `?` conversion.) Confirmed on a real corpus cart (`homunculus-0.p8.png`: `if aj>30then?"bY: nERDY tEACHERS",N.x+20,N.y+60,13`). Fixed by checking whether the byte immediately preceding the insertion point is alphanumeric/underscore and inserting a single space first when it is. | `src/preprocessor.rs` (`process_line`)

Corpus re-sweep: **111/188 clean** (up from 109) — **zero regressions**. Two full clean fixes (`picopelago-0.p8.png`, `solarharvester1kgj-2.p8.png`); several more of the 11 sampled carts progressed past their original error into a later stage (`feathered_escape_bas2-5.p8.png`, `kalikan_stage_1b-3.p8.png`: now `INIT_ERROR`; `pico_1k_jam_2023-0.p8.png`, `sfxp-1.p8.png`: now `TIMEOUT`) without yet becoming fully clean.

### Follow-up: pushing toward 100% clean — two more preprocessor bugs (round 5)

Kept mining the `LOAD_ERROR` cluster toward the 100%-clean goal, sampling the `unexpected token in expression` group (11 carts).

- **A bare digit was accepted as the start of a "new glued-together statement"'s LHS identifier — fixed.** `extract_rhs`'s heuristic for detecting size-golfed carts that jam two statements onto one line with no separator (real `x=1y=2`, no space) scans backward from an `=` sign to see if what precedes it looks like a valid assignment target — but it accepted a bare numeral as a valid "identifier," even though Lua identifiers can never start with a digit. This misfired on RHS expressions containing a numeric comparison, e.g. `h%1==0and 1or 0`: it mis-cut the RHS down to just `h%`, right before the `1`, leaving `1==0 and 1 or 0` dangling outside the parens as broken syntax. Confirmed on a real corpus cart (`pico1karena-0.p8.png`: `a[i]+=h%1==0and 1or 0`). Fixed by requiring the scanned-back candidate to actually start with an alphabetic character or `_`. | `src/preprocessor.rs` (`extract_rhs`)
- **The `@`/`%`/`$` peek-shortcuts' address-expression capture didn't stop before `&`/`|`/`^^` — fixed.** These three are pico-r's own dialect-sugar bitwise operators (rewritten to `band`/`bor`/`bxor` calls in this same preprocessing pass), and `@addr^^mask` needs the shortcut to bind only to `addr`, then apply `bxor` to the *peek() result* — but `extract_simple_expr` didn't include any of `&`, `|`, or the doubled `^^` in its stop set, so it swallowed the whole `addr^^mask` as the peek address instead. Confirmed with a diagnostic oracle probe that poked distinguishable marker values at both the "tight-binding" and "loose-binding" candidate addresses for `|`, `&`, and `^^` — official PICO-8 uses tight binding for all three (`peek(addr) op mask`), matching neither of pico-r's two prior behaviors. Critically, **plain `^` (real Lua exponentiation, not dialect sugar) empirically confirmed to behave the *opposite* way** — `@addr^2` really does mean `peek(addr^2)` (verified by reading back the exact wrapped/masked address that formula produces), so `^` deliberately was NOT added to the stop set alongside its doubled form. Confirmed real-world impact on a real corpus cart (`dontdig-1.p8.png`: `poke(24374,@24374^^32)`). | `src/preprocessor.rs` (`extract_simple_expr`)

Corpus re-sweep: **112/188 clean** (up from 111) — **zero regressions**. One full clean fix (`gunturtle_cafe-0.p8.png`); three more carts progressed past their original parse error into a later stage (`dontdig-1.p8.png`, `exterra-1.p8.png`: now `INIT_ERROR`; `hotwax-5.p8.png`: now `DRAW_ERROR`) without yet becoming fully clean. The remaining carts in the `unexpected token`/`expected 'end'` clusters weren't all individually root-caused this round — most still fail on distinct, not-yet-sampled issues further into their (often huge, single-line) source.

### Follow-up: pushing toward 100% clean — `_ENV` was completely unimplemented (round 6)

Sampled the `attempt to index a nil value` cluster (8 carts, all failing during top-level chunk execution rather than at parse time). Found that 6 of the 8 share a common "class helper" utility snippet (`new_type`/`class_functions`, traceable to a "sPACE-tAXI rEMAKE" shared source comment) that defines globals dynamically via `_ENV[name] = function...` — real Lua 5.2 syntax PICO-8 supports.

- **`_ENV` was not implemented as a global at all — fixed.** `resolve("_ENV")` fell through to a plain globals-table lookup for the literal key `"_ENV"`, which was never set, so it evaluated to `nil` and any cart indexing it (`_ENV[n] = ...`) crashed with "attempt to index a nil value". Before implementing, checked the (non-obvious) exact semantics against the oracle rather than assuming stock Lua 5.2 behavior (where the top-level chunk's `_ENV` upvalue and `_G` are literally the same table): confirmed `_ENV == _G` is **false** in official PICO-8, even though writes/reads through `_ENV[k]` still observably affect real globals. Implemented as a genuinely separate table (`Interp::env_proxy`, a distinct `Rc` identity from `globals`) registered as the global `_ENV`; `table_get`/`table_set` special-case this identity via `Rc::ptr_eq` and forward straight to the real globals table instead of storing into the proxy's own (otherwise-empty) storage. | `src/pico_lua/interp.rs` (`Interp::env_proxy`, `table_get`, `table_set`), `src/pico_lua/mod.rs` (`LuaImpl::new`)

Corpus re-sweep: **119/188 clean** (up from 112) — **zero regressions**, the single biggest win of the whole 100%-clean push so far. Seven full clean fixes (`exterra-1.p8.png`, `hewobuyadi-0.p8.png`, `kaizoleste-1.p8.png`, `mademaker-0.p8.png`, `resorte-3.p8.png`, `switch-2.p8.png`, `ultimate_selfie-2.p8.png`); two more progressed further (`fakogejuzo-0.p8.png`: now `TIMEOUT`; `kalikan_stage_1b-3.p8.png`, `space_taxi-2.p8.png`: now `UPDATE_ERROR`) without yet becoming fully clean. `super_world_of_goo-6.p8.png` (the one cluster member without the `_ENV` snippet) still fails on a distinct, not-yet-diagnosed nil-index bug.

### Follow-up: pushing toward 100% clean — `if`/`elseif` accept `do`, and a short-if separator-detection rewrite that caught its own regression (round 7)

Re-sampled the `expected 'then'` cluster again, plus revisited the standing "`if(cond) do`" finding flagged (but not fixed) two rounds ago.

- **`if`/`elseif` accept `do` as an alternative to `then` — fixed at the parser level.** Confirmed against official PICO-8 that this works both with and without parens around the condition, and for `elseif` too — but, notably, `while` does **not** accept `then` in place of `do` (verified both directions with dedicated oracle probes before assuming symmetry). Fixed by having the parser's `if`/`elseif` separator check accept either token, rather than trying to handle it as preprocessor text sugar. Confirmed real-world impact (`build_a_jetpack-1.p8.png`: `if gd.lvl <= -4 do ... end`, no parens at all). | `src/pico_lua/parse.rs` (new `expect_then_or_do`, used by `parse_if`)
- **The short-if "does this line already have an explicit separator?" check was rewritten, and the rewrite itself introduced — then had corrected — a real regression, caught by this project's own full-corpus-sweep-every-round discipline before it ever reached `main`.** The original `has_separator_keyword` helper scanned the *entire rest of the line* for a bare `then` anywhere, which broke on a short-if whose body is itself a nested compound statement with its own `then` (`if(a) if b then c end` — real carts: `build_a_jetpack-1.p8.png`, `tinyhawk-2.p8.png`). A first fix narrowed the check to "does `then`/`do` appear immediately after the first balanced paren group" — but a full re-sweep caught that this **regressed ten previously-clean carts**, because a condition can legitimately continue past a parenthesized prefix via indexing or comparison operators before its real `then` (`if (t()-last_sort_at) > 0.1 then`, `if ({...})[k] then` — real carts: `sheeple-0.p8.png`, `kaizoleste-1.p8.png`). The regression was caught by the mandatory full-sweep-before-commit step and never committed. The actual fix (`find_own_separator`) scans the real condition expression forward through operators/indexing (paren/bracket/brace depth-tracked) and, separately, tracks *nesting* depth of `if`/`while`/`for` keywords closed by `end` — not by their own `then`/`do`, since one nested `if...elseif...elseif...end` can contain several `then`s that all belong to that one statement until its `end`. A `then`/`do` only counts as this statement's own separator when found at nesting depth 0. This version was re-verified against all ten previously-regressed carts, the two original nested-if-body carts, and a full corpus sweep before being accepted. | `src/preprocessor.rs` (`find_own_separator`, replacing the removed `has_separator_keyword`; used by `expand_short_ifs`)

Corpus re-sweep (final, verified-safe version): **120/188 clean** (up from 119) — **zero regressions**. One full clean fix (`tinyhawk-2.p8.png`); `driftmania-5.p8.png` progressed further (now `TIMEOUT`). Smaller net count gain than some earlier rounds, but this round's real value was catching and correcting a regression-prone rewrite before it landed — a reminder that the full-corpus-sweep-every-round discipline is load-bearing, not a formality.

### Follow-up: pushing toward 100% clean — `extract_lhs` could splice two separate statements together (round 8, another near-miss caught by the sweep)

Sampled the `syntax error: expected call or assignment` cluster, which pointed at `extract_lhs` (the backward-scanning helper compound-assign and bitwise-op transforms use to find their left operand) reaching backward across a statement boundary it shouldn't have.

- **`extract_lhs`'s backward scan could walk through a `}` table-constructor or a call-sugar string that actually belonged to a separate, already-finished PRIOR statement — fixed.** Two real corpus carts hit this in different ways: `local d=rnd{-1,1}n-=d*cos(e)` (samurise-1.p8.png) had `n-=`'s LHS extraction walk backward through the closing `}` of the *previous* statement's `rnd{-1,1}` call, then through "rnd" itself, producing a bogus LHS (`rnd{-1,1}n`) that duplicated the entire prior call into the output; `local n="0123456789abcdef"b..="tail"` (blood_of_vladula-0.p8.png) hit the same thing via a closing quote instead of a brace. First fix attempt: a blanket "don't consume past a bare `=`" guard — caught its own regression before commit (see below). The actual fix has two parts: (1) a `}`-closed table constructor is **never** a valid assignment target in Lua at all, so `extract_lhs` no longer consumes `}` in any case; (2) a closing quote is only genuine string-call sugar (`f"str"`) when an identifier actually precedes its matching opening quote — otherwise the string is just the tail of an unrelated finished statement and must not be consumed. | `src/preprocessor.rs` (`extract_lhs`)
- **The first attempt at this fix (a generic "stop if consuming this delimiter would expose a bare `=`" guard) itself broke a working, unrelated case, caught by the mandatory full-test-suite-before-sweep step (not even the sweep — `cargo test` alone failed 3 conformance probes outright).** `h=(h*31+peek(a))&0x7fff` (from the harness's own `smoke` probe) legitimately has `=` immediately before the parenthesized group that's `&`'s left operand — that's simply where the current statement's value expression starts, not a crossed statement boundary, and the original code already terminates correctly there one iteration later (the unmatched `=` falls through to the default `break` case). The generic guard couldn't tell these apart because both look identical from "is there a bare `=` right before this delimiter" — the real distinguishing signal is specific to *why* each delimiter type is being consumed: parenthesized/bracketed groups are legitimate operands on their own regardless of what precedes them, while quote/brace *call-sugar* specifically requires a genuine callee identifier before it. Caught immediately by running the full test suite (not just the corpus sweep) before proceeding — a reminder to run the cheap, fast check first even when a slower, broader one is coming next.

Corpus re-sweep (final, verified-safe version): **121/188 clean** (up from 120) — **zero regressions**. One full clean fix (`blood_of_vladula-0.p8.png`); two more progressed further (`samurise-1.p8.png`: now `TIMEOUT`; `sb_1800_v1-2.p8.png`: now `UPDATE_ERROR`).

### Follow-up: pushing toward 100% clean — two missing host-integration API functions (round 9)

Sampled the `attempt to call a nil value` cluster (3 carts).

- **`_set_fps(n)` and `serial(addr,len,channel)` were completely unimplemented — fixed as no-op stubs.** Both confirmed real (not phantom) PICO-8 API functions via oracle probes (neither errors when called). `_set_fps` overrides the target frame rate to an arbitrary value; its effect on `stat(7)` couldn't be pinned down (a probe calling `_set_fps(45)` still read back `stat(7)` as 30 every frame), and this engine's frame loop doesn't do real-time pacing at all regardless (run-cart/lib.rs just step a fixed number of frames), so a no-op is the correct/only meaningful behavior here. `serial()` is host GPIO/serial data export with no meaningful effect without an actual host-side channel to write to. Both stubbed following the existing `menuitem`/`extcmd`/`flip` no-op pattern. | `src/pico_lua/api.rs` (`api_set_fps`, `api_serial`)

Corpus re-sweep: **121/188 clean** (unchanged) — **zero regressions**, but real progress: two carts that previously crashed immediately at load now run substantially further (`bytebeat_tweet-0.p8.png`, `pico1kmatch-0.p8.png`: both now `TIMEOUT` instead of `LOAD_ERROR`). `pico1kshmup_1-0.p8.png` (the third cluster member) hit a different, unrelated bug (`arith on nil`) further in, not chased this round.

### Follow-up: pushing toward 100% clean — a bare `~` wrongly terminated a bitwise RHS scan (round 10)

Sampled the `lex error: unexpected character 0x26` cluster ('&', meaning a `band`/`bor` conversion silently failed to fire and left the raw operator in the output).

- **`extract_bitwise_rhs` treated a bare `~` as an unconditional stop character, which broke `x & ~y` (unary bitwise-NOT as the right operand) — fixed.** `~` is PICO-8's unary bitwise-NOT prefix (`~x` -> `bnot(x)`) as well as the first character of the `~=` comparison operator; the RHS-boundary stop-list included a bare `~`, presumably meant only to guard against swallowing a trailing `~=`. But when `~` is the very *first* character of the RHS itself (`mouseb & ~mouseb_last`), the stop fired immediately (after only skipping leading whitespace), producing a completely empty RHS -- `try_bitwise_op` bails out on an empty RHS, so the `&` was left unconverted and reached the real Lua lexer as a raw, unsupported character. Fixed by only stopping on `~` when it's actually followed by `=`. Confirmed on a real corpus cart (donsol8_v1-14.p8.png: `mouseb_down = mouseb & ~mouseb_last`). | `src/preprocessor.rs` (`extract_bitwise_rhs`)
- **Investigated but did NOT change: `number_to_str`'s "-0 collapses to 0" behavior.** While chasing this, found via a diagnostic oracle probe that official PICO-8 actually prints "-0" for `bnot(0)` (and any bitwise expression that reduces to it) -- seemingly contradicting the *existing*, already oracle-verified `number_format.p8` probe, which confirms official prints "0" for the literal `-0.00001`. Both are real, oracle-confirmed, but the underlying reason they differ requires true 16.16 fixed-point quantization to distinguish: `bnot(0)` produces an exact, already-fixed-quantized nonzero raw value (-1/65536) via bit manipulation, while a literal like `-0.00001` is presumably truncated to exact zero *at parse time* on real hardware (this engine currently keeps it as a genuine tiny non-zero `f64`, since numbers are stored as full-precision floats rather than true 16.16 fixed integers throughout -- a known, deferred architectural gap, now tracked as a standing follow-up task). Removing the "-0" collapse fixes the `bnot(0)` case but regresses the already-locked-in literal case, so it was left as-is (matching the more common ordinary-tiny-float case) rather than risk a real regression to fix a narrower, rarer one -- documented in a code comment for whoever eventually does the full quantization work.

Corpus re-sweep: **122/188 clean** (up from 121) — **zero regressions**. One full clean fix (`donsol8_v1-14.p8.png`).

### Follow-up: pushing toward 100% clean — `@`/`%`/`$` fusing with a preceding keyword, and a near-miss regression from the same fix (round 11)

Sampled the `unexpected token in expression` cluster; found the same "dialect sugar glued onto a keyword" class of bug as the earlier `?`/`then` fix, this time for the peek-shortcuts.

- **`@`/`%`/`$` glued directly onto a preceding keyword with no separator fused into a bad identifier — fixed, same pattern as the earlier `?`-after-keyword fix.** `try_peek_shortcut` spliced `peek(`/`peek2(`/`peek4(` in verbatim after whatever preceded the shortcut character, so `if$24064~=0then` (no spaces anywhere) became `ifpeek4(24064)~=0then`. Fixed the same way: insert a space when the preceding output byte is alphanumeric/underscore. Confirmed on a real corpus cart (redash-7.p8.png: `if$24064~=0then?"..."`). | `src/preprocessor.rs` (`try_peek_shortcut`)
- **That fix immediately regressed a previously-clean cart — caught by the mandatory full-sweep-before-commit step, root-caused, and fixed properly rather than papered over.** `batazubipe-0.p8.png` has a high-byte P8SCII glyph directly followed by `%40` (`if <glyph>%40<15 then`). The glyph expands to a synthetic `_p8_XX` identifier, but `is_prev_value` (which decides whether a `%` is modulo vs. the peek2-shortcut) only recognized ASCII alphanumeric characters as "value-like" context -- a raw high-byte glyph byte isn't ASCII-alphanumeric, so `%` here was misclassified as the peek2-shortcut even though it's genuinely modulo on the glyph-derived value. Before this round's fix, the resulting fused-but-syntactically-valid text (`_p8_..._p8_bdpeek2(40)`, parsed as a call to a nonexistent function) happened not to error during the cart's actual run; after adding the space, it became `_p8_..._p8_bd peek2(40)` -- two adjacent tokens with no operator between them, a genuine parse error. The real fix was one level deeper: teach `is_prev_value` that a high-byte glyph byte counts as value-like context too, so `%` here is correctly left as plain modulo in the first place (no peek2 conversion, no space-insertion question ever arises). Verified against `batazubipe-0.p8.png` plus the two other carts the broken intermediate version had also regressed further into. | `src/preprocessor.rs` (`is_prev_value`)

Corpus re-sweep (final, verified-safe version): **122/188 clean** (unchanged) — **zero regressions**. `redash-7.p8.png` progressed past its original error into a later, different one (still `LOAD_ERROR`, not yet fully clean).

### Follow-up: pushing toward 100% clean — `load()` and `tline()` were missing entirely (round 12)

Sampled the `INIT_ERROR` cluster (8 carts): 4 are blocked on `cocreate` (the known coroutine architectural gap), but the other cases pointed at two more completely-missing API functions.

- **`load(filename)` (multi-cart "warp" switch) and `tline(...)` (textured line) were both unimplemented, crashing any cart that calls them — fixed as no-op stubs.** Both confirmed real via oracle probes (`load` targeting a nonexistent cart doesn't even error — execution just continues past it). `load` can't be meaningfully implemented in an engine that only ever runs one cart per invocation; `tline`'s real raster algorithm needs dedicated oracle verification against actual sprite/map data before implementing (a wrong guess would silently corrupt every textured-line cart's screen output), so no-op is the honest intermediate state for both — same pattern as `menuitem`/`extcmd`/`flip`/`_set_fps`/`serial`. | `src/pico_lua/api.rs` (`api_load`, `api_tline`)

Corpus re-sweep: **128/188 clean** (up from 122, the second-biggest single round of the whole push) — **zero regressions**. Six full clean fixes: the two directly-targeted carts (`solitomb-2.p8.png`: `load`, `tomorrow-6.p8.png`: `tline`) plus four more whose DRAW/UPDATE-stage crashes turn out to have been tline-calls too (`gugepanofu-6.p8.png`, `space_taxi-2.p8.png`, `tmnt_sp_start-0.p8.png`, `yet_another_mountain-2.p8.png`). A real (non-stub) `tline` implementation remains flagged as follow-up work — these six carts now run clean but any textured-line visuals they draw are missing from the framebuffer.

### Follow-up: pushing toward 100% clean — three more preprocessor bugs from the `expected 'end'` cluster (round 13)

Root-caused the `expected 'end'` cluster (7 carts) — three distinct bugs, all found via one cart (`dinkykong-0.p8.png`/`hakai-3.p8.png`) and each confirmed to affect others:

- **A short-if body ending in a `--` comment buried the synthetic `end` inside the comment — fixed.** `expand_short_ifs` appends its pending synthetic `end`s after the main copy loop, but the loop's comment branch copies `--...` to end of line first — so the appended `end` landed inside the comment text (`if(c)return true --todo:` became `if c then return true --todo: end`). Fixed by emitting pending `end`s *before* the comment. This exposed a latent second bug in the same function: the "did we transform anything?" check at the end tested `ends_needed == 0`, which the comment path now zeroes after emitting — a fresh `expanded_any` flag keeps the transformation from being silently discarded. | `src/preprocessor.rs` (`expand_short_ifs`)
- **A `?` print-shorthand appearing MID-body (after other statements) swallowed the synthetic `end` — fixed.** The body-START case (`if(cond)?args`) was fixed in an earlier round, but `if(ob.sel)rect(...) ?ob.x..","..` copies the `?` through verbatim, and process_line's later `?`-conversion (which consumes to end of line) then swallowed the appended `end` into print's argument list. Fixed by converting a mid-body `?` inside `expand_short_ifs` itself, mirroring the body-start case: `print(<args up to any trailing comment>)`, then the pending `end`s, then any comment. | `src/preprocessor.rs` (`expand_short_ifs`)
- **`extract_lhs` walked back through `..` (concat) as if it were a field-access dot — fixed.** The identifier-chain scan accepts `.` for `a.b.c` chains, so `"time:"..timer\1` walked the int-div's LHS back through the concat operator into the string, splicing out `..timer` as the operand (`print("time:"flr(..timer/(1)))`). Fixed by stopping the scan when two consecutive dots are found. | `src/preprocessor.rs` (`extract_lhs`)
- **`basicshmupdc1-0.p8.png` confirmed genuinely broken on official PICO-8 too — not a pico-r gap.** Its source opens a `--[[` long comment at line ~335 that is never closed; the official binary fails with "unfinished long comment near <eof>" on the same cart (verified headlessly). This cart can never run "clean" anywhere; a load error IS the conformant behavior. First confirmed member of a "broken on real hardware too" list — relevant to interpreting the 100%-clean goal, since such carts cap the achievable maximum below 188 unless counted as conformant-by-matching-error.

Corpus re-sweep: **133/188 clean** (up from 128) — **zero regressions**. Five full clean fixes (`deskstop30-0.p8.png`, `dinkykong-0.p8.png`, `motionrec-3.p8.png`, `nemonemo-4.p8.png`, `ratdrms-0.p8.png`); three more progressed (`hakai-3.p8.png`, `pong_xmas-0.p8.png`: now `INIT_ERROR` — hakai's is the known cocreate/coroutine gap; `picowings_2-0.p8.png`: now `TIMEOUT`).

### Follow-up: pushing toward 100% clean — `//` comments, `;` RHS stop, comment-in-bitwise-RHS (round 14)

Before this round, ran ALL 21 remaining LOAD_ERROR carts through the official binary as a batch classification: only `basicshmupdc1-0.p8.png` fails on real hardware (the unterminated long comment above) — every other remaining LOAD_ERROR is a genuine pico-r gap. Also added an opt-in `PICOR_PARSE_DEBUG=1` env flag to the parser's error path that dumps the tokens around a failure, which immediately localized several bugs inside multi-thousand-character golfed lines.

- **PICO-8 accepts C-style `//` line comments — completely unimplemented, fixed.** Confirmed via oracle. Raw `//` reached the real Lua lexer as two division operators (or, when the comment text contained a quote, produced phantom "unterminated string" lex errors). Fixed in `process_line` (rewrites `//` to `--` so the lexer accepts it) plus the three other scanners that must treat it as a hard stop like `--` (`expand_short_ifs`'s pending-`end` flush, `extract_rhs`, `extract_bitwise_rhs`, `find_own_separator`). | `src/preprocessor.rs`
- **`;` didn't terminate a compound-assign's RHS capture — fixed.** `i+=1;return n[i]` captured `1;` as the RHS and spliced the semicolon inside the rewritten parens (`i = i + (1;)`). Confirmed on a real corpus cart (`picketpuzzle-5.p8.png`). | `src/preprocessor.rs` (`extract_rhs`)
- **A `--` comment following a bitwise op's RHS was smuggled into the rewritten call — fixed.** `f = 1 << 3--will not swap` captured `3--will` as the RHS (the digit-then-minus sequence isn't a stop in `extract_bitwise_rhs`, since binary minus is legitimate there), and once wrapped as `shl(1,3--will)` the comment ate the closing paren. Confirmed on a real corpus cart (`swap_mouse-8.p8.png`). | `src/preprocessor.rs` (`extract_bitwise_rhs`)

Corpus re-sweep: **139/188 clean** (up from 133) — **zero regressions**. Six full clean fixes (`cross_battle_wip1-0.p8.png`, `jelpi_random-0.p8.png`, `picketpuzzle-5.p8.png`, `slimer2-0.p8.png`, `swap_mouse-8.p8.png`, `toyboxgeneral-0.p8.png` — the last's "unterminated string" was a quote inside a `//` comment); `mot_pool-23.p8.png` progressed to `DRAW_ERROR`.

### Follow-up: pushing toward 100% clean — second-comparison mis-split and value juxtaposition (round 15)

- **`extract_rhs`'s glued-statement heuristic fired on the FIRST `=` of a `==` comparison later in the RHS — fixed.** The heuristic recognizes `identifier=` as a new golfed-on assignment statement, and excluded `==` only by looking backward from the *second* `=` — the first `=`, preceded by an ordinary identifier, looked exactly like an assignment. An and/or ternary chain with two comparisons (`i.dx+=i.go=="w"and-1or i.go=="e"and 1or 0`) got mis-split at the second `==`, leaving `or)` with a missing operand. Fixed with a one-token lookahead: `==` skips both chars. Confirmed on a real corpus cart (`puzzlesofthepaladin-3.p8.png`). | `src/preprocessor.rs` (`extract_rhs`)
- **`extract_lhs` walked back through a closing delimiter that directly abuts the captured identifier — fixed.** In `e.y-=rnd"18"e.a+=rnd"6"` (two golfed-together compound assigns), the second `+=`'s LHS scan captured `e.a`, then saw the `)` of the first statement's expansion (`... - (rnd"18")`) and consumed straight through it as if it were a call chain — but a closing `)`/`]`/quote directly followed by an identifier character is value *juxtaposition* (two separate statements), never a valid single-LHS chain (a real chain always connects via `.`/`[`/`(`). Fixed by stopping the scan in exactly that configuration. Confirmed on a real corpus cart (`praxis_fighter_x-2.p8.png`). | `src/preprocessor.rs` (`extract_lhs`)

Corpus re-sweep: **141/188 clean** (up from 139) — **zero regressions**. Two full clean fixes (`puzzlesofthepaladin-3.p8.png`, `lab_cat-6.p8.png`); `oblivion_eve-12.p8.png` progressed to `INIT_ERROR`. `praxis_fighter_x-2.p8.png` progressed past all its parse errors but now needs real Lua 5.2 `_ENV`-as-local scoping (`for _ENV in all(n) do` — locals named `_ENV` must change global name resolution inside their scope), a genuine language feature well beyond the current global-proxy `_ENV` implementation — flagged for a future round.

### Follow-up: pushing toward 100% clean — five more fixes and two intra-round regressions caught (round 16)

- **A number glued to a high-byte glyph fused into one identifier — fixed.** `insert_number_spaces` only inserted the separating space when a number was followed by an ASCII letter; a glyph (identifier character in PICO-8, expanded to `_p8_XX` later) got fused (`-2<glyph>.y-=8`, `redash-7.p8.png`), corrupting a later LHS extraction. | `src/preprocessor.rs` (`insert_number_spaces`)
- **`!` (of `!=`) wasn't a stop in the bitwise/simple RHS scanners — fixed.** `btn()&15!=x` captured `15!` into the rewritten `band()` and the comparison never converted (`deepening-0.p8.png`). | `src/preprocessor.rs` (`extract_bitwise_rhs`, `extract_simple_expr`)
- **A short-if "body" starting with a binary operator + operand is a multi-line condition — fixed.** `is_continuation_body` only recognized a body that was *nothing but* a dangling operator; `if (abs(en.x-64))>80` / next line `or ... then` mis-expanded (`deepening-0.p8.png`). Now any body *starting* with a binary operator counts (no Lua statement can begin with one). | `src/preprocessor.rs` (`is_continuation_body`)
- **`%` after whitespace mis-classified as peek2 shortcut — fixed, then the fix's own regression caught and root-caused.** `is_prev_value` only looked at the immediately-preceding byte, so `timer %2==0` (space before `%`) read as the peek2 shortcut. Fixed by skipping whitespace backward — which the same-round sweep showed broke `return %addr` (`pizza_panda-1.p8.png`: `return` ends in identifier chars but is a keyword, not a value). Final version skips whitespace AND rejects non-value keywords (`true`/`false`/`nil` stay value-like). | `src/preprocessor.rs` (`is_prev_value`)
- **`tonum(true)`/`tonum(false)` returned nil — fixed to 1/0, confirmed via oracle.** Carts use `tonum(btn"1")-tonum(btn"0")` for directional input (`deepening-0.p8.png`). | `src/pico_lua/api.rs` (`api_tonum`)
- **Runaway Lua recursion crashed the whole host process — fixed with a 1000-frame call-depth cap raising official PICO-8's "out of memory" as a catchable cart error.** Caught because this round's fixes let `minilegacy-0.p8.png` run further than ever before, into unbounded recursion that overflowed the native stack (SIGABRT, exit 134) — the tree-walking interpreter spends native stack per Lua call level, so this was a latent host-crash bug reachable by any deeply-recursing cart. Verified against the oracle that infinite recursion on real hardware reports "out of memory" as a cart error. | `src/pico_lua/interp.rs` (`call_lua_closure`)

Corpus re-sweep: **143/188 clean** (up from 141) — **zero regressions**. Two full clean fixes (`deepening-0.p8.png`, `sb_1800_v1-2.p8.png`); `minilegacy-0.p8.png` and `pizza_panda-1.p8.png` restored to their pre-round states with the intra-round regressions fixed properly rather than reverted around.

### Follow-up: pushing toward 100% clean — real `local _ENV` scoping; `_G` is a phantom (round 17)

- **Real Lua 5.2 `_ENV` scoping implemented — a local/parameter/loop-variable named `_ENV` now redirects free-name reads AND writes in its scope through that table (respecting `__index`).** OOP-golfed carts use `function f(_ENV)` / `for _ENV in all(t)` / `local _ENV=obj` pervasively (`praxis_fighter_x-2.p8.png`, `redash-7.p8.png`). Implementation: `resolve`/`assign_name` check for a visible `_ENV` slot after the normal local lookup misses, and go through `table_get`/`table_set` (metamethod-aware) instead of the globals table. Closures already capture all visible locals by name, so a bound `_ENV` flows into nested functions automatically. Ordinary carts never bind a local `_ENV`, so the extra check misses and behavior is unchanged. | `src/pico_lua/interp.rs` (`resolve`, `assign_name`)
- **`_G` does NOT exist in official PICO-8 — removed.** Confirmed via oracle (`tostr(_G)` prints "[nil]"): another phantom feature, like `pcall`/`error`/the string library removed earlier this session. Real carts chain metatables to a *captured top-level `_ENV`* instead (which official confirms is a real table) — that idiom now works through the env-proxy special-casing. The internal `all()`/`foreach()` env-fallback shim was the only `_G` consumer and now uses `__index=_ENV`. | `src/pico_lua/mod.rs`
- Oracle probe verified all four scoping shapes byte-for-byte against official: parameter `_ENV`, `do local _ENV=t`, `for _ENV in all(t)`, and `_G` being nil.

Corpus re-sweep: **145/188 clean** (up from 143) — **zero regressions**. Two full clean fixes (`feathered_escape_bas2-5.p8.png`, `pong_xmas-0.p8.png` — both INIT_ERROR carts whose failing helper functions turn out to be `_ENV`-scoped class methods). `praxis_fighter_x-2.p8.png` and `redash-7.p8.png` progress further still but now fail inside cart-embedded decompressor loops that appear to need exact 16.16 fixed-point arithmetic (the deferred quantization epic, being worked in a parallel session) — not chased here.

### Follow-up: pushing toward 100% clean — a frame budget for explicit-flip main loops (round 18, biggest round of the whole push)

The TIMEOUT category (17 carts) was dominated by carts that never define `_update`/`_draw` at all — old-style carts run their entire game as `::_:: ... flip() goto _` at top level, which official PICO-8 runs forever *by design*. Under a headless fixed-frame harness these can never terminate; they were being killed by the watchdog and counted as failures even when the game loop itself was executing perfectly. The fix reframes them honestly: give the harness a frame budget that ends the run cleanly after the loop has demonstrably run N frames.

- **`flip()` now does real frame accounting (`flip_count`/`frame_count`/`elapsed_time`), and a host-set `flip_limit` raises a marker error past the budget, which run-cart reports as "explicit-flip main loop ran N frames clean" (exit 0).** `flip_limit=0` (the default) disables all of this for interactive/WASM hosts. | `src/pico_lua/api.rs` (`api_flip`, `FLIP_LIMIT_MARKER`), `src/state.rs`, `tools/run_cart.rs`
- **P8SCII frame-pause control codes count as frames too.** Golfed carts replace flip() entirely with `?"\^1\^c"` (pause-one-frame + clear-screen control codes inside a print string — `lv-2.p8.png`); api_print scans for `\^1`..`\^9` and ticks the same budget. | `src/pico_lua/api.rs` (`api_print`)
- **Sustained `btn()`/`btnp()` polling counts as synthetic frames.** Input-wait busy loops (`while(not btn(4))do print(...)end`, `onek_landscape06-0.p8.png`) poll without ever flipping; official keeps servicing frames during such loops so they terminate on real hardware. Headlessly, every 64 polls now tick one synthetic frame, so the same budget ends the wait loop. | `src/pico_lua/api.rs` (`note_btn_poll`)
- **run-cart's title-screen simulation now presses X for 8 frames then O for 8 frames** (carts vary in which button their "press to start" waits for). | `tools/run_cart.rs`

Corpus re-sweep: **157/188 clean** (up from 145 — twelve carts in one round, the biggest single gain of the entire session) — **zero regressions**. All twelve were TIMEOUT carts whose real main loops now run their full frame budget cleanly: `advent_challah-11`, `celesteprogrupter-2`, `celesteprogrupter-3`, `grad1k-0`, `lv-2`, `onek_landscape06-0`, `pico1kmatch-0`, `pico_1k_jam_2023-0`, `picowings_2-0`, `secretsanta-0`, `sfxp-1`, `solongsofar1-0`. Only 5 TIMEOUTs remain (`bytebeat_tweet-0`: a `while stat(108)<1000` serial-drain loop; `samurise-1`, `fakogejuzo-0`, `terra_1cart-42`, `driftmania-5` progressed to UPDATE_ERROR), plus 10 LOAD / 7 INIT / 6 DRAW / 3 UPDATE errors.

### Follow-up: pushing toward 100% clean — real coroutines (round 19)

The last big architectural gap. `cocreate`/`coresume`/`costatus`/`yield` are now fully implemented for native builds via OS threads with strict baton-passing: at any instant exactly one thread (main or one coroutine) executes interpreter code; every handoff is a channel send/recv pair (the happens-before edge that makes sharing the Rc/RefCell interpreter state sound), and the interpreter's single Lua frame stack is swapped at each handoff so each side sees exactly its own call stack. A coroutine discarded while suspended parks its thread until process exit (accepted, documented); wasm builds have no threads, so `cocreate` surfaces a Lua error there instead of aborting. The Lua-visible coroutine value is a registry-keyed table (official reports `type()=="thread"`; no corpus cart observed relying on that). Verified against the oracle across the whole lifecycle byte-for-byte — statuses, bidirectional value passing (resume args → yield returns and back), final return value, resuming a dead coroutine (`false, "corolib: cannot resume dead coroutine"` — message matched exactly including official's `corolib:` prefix), and error-in-coroutine reporting `false` — locked in as `probes/coroutines.p8`. | `src/pico_lua/coroutine.rs` (new), `src/pico_lua/interp.rs`, `src/pico_lua/api.rs`

Corpus re-sweep: **160/188 clean** (up from 157) — **zero regressions**. Three full clean fixes (`dontdig-1.p8.png`, `hakai-3.p8.png`, `spiderbat-0.p8.png`); `buttworm-0.p8.png` progressed from INIT_ERROR to DRAW_ERROR (a different, non-coroutine bug).

### Follow-up: pushing toward 100% clean — binary `~` is XOR; `inext` builtin (round 20)

- **Binary `~` (a value on its left) is bitwise XOR, exactly like `^^` — confirmed via oracle (`5~3` == 6) and implemented.** The preprocessor previously treated every non-`~=` tilde as unary bnot, so `j~i` became the glued call `jbnot(i)` ("attempt to call a nil value"). Now `~` with value-like context to its left routes through the same `try_bitwise_op`/`bxor` path as `^^`; the unary form is unchanged. Real corpus carts: `brandgnume-1.p8.png`, `kokoroko-3.p8.png`, `oblivion_eve-12.p8.png`. | `src/preprocessor.rs` (`process_line`)
- **`inext(t, i)` — the sequence-indexed iterator companion to `next()` (`for i,v in inext,t`) — was missing entirely; implemented and oracle-confirmed.** Returns `i+1, t[i+1]` until the first nil slot. Real corpus carts: `kalikan_stage_1b-3.p8.png`, `redash-7.p8.png`. | `src/pico_lua/api.rs` (`lua_inext`)

Corpus re-sweep: **163/188 clean** (up from 160) — **zero regressions**. Three full clean fixes (`brandgnume-1.p8.png`, `kalikan_stage_1b-3.p8.png`, `oblivion_eve-12.p8.png`); `kokoroko-3.p8.png` progressed past the XOR crash to an unrelated string-concat nil.

### Follow-up: pushing toward 100% clean — `#` prefix in int-div LHS; stat date/time (round 21)

- **`#` (length) belongs INSIDE a captured int-div/bitwise operand — fixed.** `#levels \ levels_per_row` rewrote as `#flr(levels/(n))` (length of a number — runtime error) because `extract_lhs` stopped before the `#`; it's always a prefix operator binding tighter than these binary ops, so it's now consumed into the operand like unary minus. Confirmed on a real corpus cart (`woodworm-0.p8.png`). | `src/preprocessor.rs` (`extract_lhs`)
- **`stat(80..85)`/`stat(90..95)` (UTC/local date-time components) returned the generic 0 fallback... actually nil-adjacent behavior crashed date-display carts — now return a fixed, plausible timestamp** (2024-01-01 12:00:00; real wall-clock values can't be deterministic in tests). Confirmed on a real corpus cart (`kokoroko-3.p8.png`: prints a `stat(92)..sep..stat(91)` clock). | `src/pico_lua/api.rs` (`api_stat`)

Corpus re-sweep: **165/188 clean** (up from 163) — **zero regressions**. Two full clean fixes (`kokoroko-3.p8.png`, `woodworm-0.p8.png`).

### Follow-up: pushing toward 100% clean — multi-line `?[[...]]` and string-sugar `%` (round 22)

- **A `?`-print whose argument is a multi-line `[[ ]]` long string closed its `print(` paren at the first physical line's end, corrupting the string — fixed.** The pending close is now carried across physical lines alongside the existing long-string state (`pending_print` flag threaded through `preprocess` → `process_line`), deferring the `)` until the string and any trailing args on the closing line finish. Confirmed on a real corpus cart (`homunculus-0.p8.png`: `?[[ ENTER ... ]],102,29,2`). | `src/preprocessor.rs`
- **`%` right after a string literal's closing quote is modulo, not the peek2 shortcut — fixed.** In operator position (these scanners only run outside string literals) a quote means a string value just ended — e.g. string-call sugar `p"n4"%6`. Added closing quotes to `is_prev_value`'s value-like set. Confirmed on a real corpus cart (`picodex_dual-1.p8.png`: `T.S[p"n4"%6+1]`, which now clears all parse errors and fails only on a deeper runtime gap). | `src/preprocessor.rs` (`is_prev_value`)

Corpus re-sweep: **166/188 clean** (up from 165) — **zero regressions**. One full clean fix (`homunculus-0.p8.png`).

### Follow-up: pushing toward 100% clean — multi-line compound-assign RHS (round 23)

- **A compound-assign RHS that leaves parens open at end of line continues on later physical lines — fixed.** `bx+=d_mbtn(` with the call's arguments on following lines had its RHS capture stop at end of line, splicing a truncated fragment into the rewrite. When the captured RHS's paren/bracket depth is still positive at EOL, the rewrite now emits `lhs = lhs op rhs` WITHOUT the usual precedence-hygiene wrapping parens — the still-open source paren makes the continuation lines flow into the same expression and close it naturally. (The function-call `^^=`-style and int-div forms need a synthesized closing paren that can't be deferred; they keep the old single-line behavior.) Confirmed on real corpus carts (`build_a_jetpack-1.p8.png`, `pony9000_1_3_3-0.p8.png`). | `src/preprocessor.rs` (`try_compound_assign`)

Corpus re-sweep: **168/188 clean** (up from 166) — **zero regressions**. Two full clean fixes (`build_a_jetpack-1.p8.png`, `pony9000_1_3_3-0.p8.png`).

### Follow-up: pushing toward 100% clean — end-to-end byte fidelity for P8SCII glyphs (round 24, foundational)

Discovered while chasing `ascent-0.p8.png`'s "arith on nil": `cart.lua_code` was a `String` produced by `from_utf8_lossy`, which converts every raw high byte (P8SCII glyphs are single bytes 0x80-0xFF, never valid UTF-8) into the SAME Unicode replacement character — **all distinct glyph identifiers in every cart collapsed into one alias**, and every glyph inside a string literal was corrupted to the same wrong character before it ever reached print(). A second lossy conversion at the preprocessor's output did the same again. This had been silently shaping behavior all session.

- **Cart source is now raw bytes end-to-end.** `Cart.lua_code: Vec<u8>`; `preprocess(&[u8]) -> Vec<u8>`; the lexer consumes bytes (it always did). No lossy conversion anywhere on the code path. | `src/cart.rs`, `src/preprocessor.rs`, `src/pico_lua/mod.rs`, tools/tests
- **Glyph identifiers now carry their exact raw bytes as their name** (lexer treats bytes ≥0x80 as identifier characters; the `_p8_XX` synthetic-name mapping is gone), so a glyph variable's table key equals a raw-byte string literal of the same glyph — `t={<glyph>=1}` and `t["<glyph>"]` are the same slot, byte-for-byte what official does (oracle-verified with a raw-byte probe). | `src/pico_lua/lex.rs`, `src/preprocessor.rs`
- **Button-glyph constant substitution is now scoped the way official scopes it** (same oracle probe): it fires in standalone expression position only — NOT when the glyph is directly followed by a single `=` (table-constructor key: `{<glyph>=split"-1,0"}` stores under the raw-byte string key), and NOT when the glyph abuts identifier characters or a preceding `.` (`nes_btn.<glyph>p` is one field name — seamonsterquest_release1c-0.p8.png). Both misfires were caught as intra-round regressions by the sweep and fixed at the root. | `src/preprocessor.rs`
- `extract_lhs` treats bytes ≥0x80 as identifier characters in its backward scan (a glyph value's int-div/bitwise rewrite captured an empty LHS otherwise).

Corpus re-sweep: **170/188 clean** (up from 168) — **zero regressions**. Three full clean fixes across the two sub-rounds (`ascent-0.p8.png` — its "arith on nil" was two distinct glyph counters aliased into one variable; `pico1kshmup_1-0.p8.png`; plus the earlier `puzzlesofthepaladin-3.p8.png` hold); `fakogejuzo-0.p8.png` progressed from TIMEOUT to DRAW_ERROR and `to_take_root_among_the_stars_1-7.p8.png` to UPDATE_ERROR.

### Follow-up: pushing toward 100% clean — backward goto dropped in-scope locals (round 25)

- **A backward `goto` truncated ALL of the enclosing block's locals instead of only those declared after the target label — fixed.** Real Lua closes only locals whose scope begins after the label; the old "restore to start-of-block" shortcut nil'd out locals a loop-via-goto still needed (`local lin=tb[y]` before `::light_ov::`, jumped back to from inside a pairs() loop — hotwax-5.p8.png). The interpreter now records the live-locals count each time a label statement is passed and truncates to that on a backward jump; a forward jump (label not yet passed) keeps current locals, since the skipped declarations simply never execute. Both directions oracle-verified (`probes/goto_preserves_locals.p8`). | `src/pico_lua/interp.rs` (`execute_block_inner`)

Corpus re-sweep: **171/188 clean** (up from 170) — **zero regressions**. One full clean fix (`hotwax-5.p8.png`). Also batch-classified all remaining non-clean carts against the official binary this round: every one except `basicshmupdc1-0.p8.png` (the known unterminated-long-comment cart) RUNS on official — so the strict-exit-code achievable maximum is 187/188, with `basicshmupdc1` conformant-by-matching-error.

### Follow-up: pushing toward 100% clean — split() hex/binary/whitespace numberization (round 26)

- **`split()`'s token→number conversion now matches official: hex (`0x6000`, with optional fraction) and binary (`0b11`) literals convert, and surrounding whitespace is ignored while parsing (`" 6"` → 6) — while a token that fails to convert keeps its original spacing as a string.** All oracle-confirmed (`split"0x10"` yields the NUMBER 16 on real hardware; `split"0b11"` yields 3; `" 6"` yields 6; `"x "` stays the exact string). Previously only Rust's plain f64 grammar was accepted, so address tables like `u"0x6000,0xe000,0x1fff"` and spaced argument packs like `u"5, 6, 16, 0, 7, 96"` (both used by `mer_ork-0.p8.png`) silently produced strings that blew up later in comparisons. | `src/pico_lua/api.rs` (`split_token_to_number`, `parse_radix_fraction`)

Corpus re-sweep: **172/188 clean** (up from 171) — **zero regressions**. One full clean fix (`mer_ork-0.p8.png`).

### Follow-up: pushing toward 100% clean — removed the all()/foreach() env-wrap shim (round 27, five carts at once)

- **The PICO-Z-inherited shim that wrapped every metatable-less table yielded by `all()`/`foreach()` with `{__index=_ENV}` was NOT official behavior and leaked globals into ordinary field misses — removed.** `n.next` on a node table without a `next` field returned the BUILTIN `next` function through the shim's metatable (truthy! then `next.pos` crashed — buttworm-0.p8.png); other carts read other accidental globals the same way, producing "arith on table", phantom recursion (minilegacy-0.p8.png's "out of memory" — a shim-leaked global made its recursion non-terminating), and assorted nil-index crashes. The shim's original purpose (letting `for _ENV in all(t)` elements reach builtins) is obsolete: real `local _ENV` scoping is implemented, and carts genuinely iterating `_ENV` set their own metatable chains to a captured outer `_ENV` (they'd crash on official otherwise). All previously-fixed `_ENV` carts verified to hold. | `src/pico_lua/mod.rs`

Corpus re-sweep: **177/188 clean** (up from 172 — five carts in one deletion) — **zero regressions**. Five full clean fixes: `buttworm-0.p8.png`, `minilegacy-0.p8.png`, `pico1karena-0.p8.png`, `pizza_panda-1.p8.png`, `to_take_root_among_the_stars_1-7.p8.png`.

### Follow-up: pushing toward 100% clean — `__unm` metamethod (round 28)

- **Unary minus now dispatches to `__unm` for non-number operands, joining the arithmetic metamethods implemented earlier.** 3D-math carts overload it on vectors (`-v` in mot_pool-23.p8.png's matrix library). Oracle-verified (`probes/metamethod_unm.p8`). | `src/pico_lua/interp.rs` (`eval_unop`)

Corpus re-sweep: **178/188 clean** (up from 177) — **zero regressions**. One full clean fix (`mot_pool-23.p8.png`). Remaining 10: `basicshmupdc1-0` (broken on official too — conformant-by-matching-error), `bytebeat_tweet-0`/`samurise-1`/`terra_1cart-42`/`driftmania-5` (TIMEOUT: stat(108)-drain loop, parens8 embedded LISP VM — likely raw interpreter throughput), `fakogejuzo-0` (DRAW_ERROR), `picodex_dual-1`/`praxis_fighter_x-2`/`redash-7`/`super_world_of_goo-6` (deep runtime divergence inside cart-embedded decompressors — the deferred exact-16.16-fixed-point epic, in progress in a parallel session).

### Follow-up: pushing toward 100% clean — `#`/unpack border semantics (round 29)

- **`#` (and `unpack()`, which uses it) now follows Lua's luaH_getn border search instead of stopping at the first nil hole.** Oracle-confirmed across five table shapes: `#{1,2,nil,4,5}` is 5 (holes spanned), sparse `u[1] u[2] u[4] u[5]` is 5, a lone `u[1000]=1` is 0, `{1,2,3}` plus `w[1000]=1` is 3, and `z[3]=1` alone is 0. The old first-hole scan silently truncated any `unpack()` destructuring of containers with nil fields (`local cx,cy,w,h,pad,... = unpack(self)` — fakogejuzo-0.p8.png, which still fails on a separate nil elsewhere but now destructures correctly). Implemented as the same doubling-probe + binary-search border find real Lua uses. | `src/pico_lua/value.rs` (`raw_len`)

Corpus re-sweep: **178/188 clean** (unchanged) — **zero regressions**; `driftmania-5.p8.png` progressed from TIMEOUT to UPDATE_ERROR.

### Follow-up: pushing toward 100% clean — stat(108) serial-queue accounting (round 30)

- **`stat(108)` reports cumulative bytes queued via `serial()` — implemented and oracle-confirmed** (each `serial(ch,addr,len)` adds `len`; previously the generic 0 fallback). This is the pacing signal `bytebeat_tweet-0.p8.png`'s audio loop watches (`while stat(108)<1000 do ... serial(...) end`); the queue now counts correctly, though that cart still can't terminate headlessly — its outer loop paces on the queue DRAINING in real time as audio plays, which has no headless analogue (documented as out-of-scope for the harness, like real-time flip pacing before the frame-budget work). | `src/state.rs`, `src/pico_lua/api.rs` (`api_serial`, `api_stat`)

Corpus re-sweep: **178/188 clean** (unchanged) — **zero regressions**.

### Follow-up: pushing toward 100% clean — devkit/serial stats are booleans (round 31)

- **`stat(30)` (devkit key pending), `stat(31)` (pending key), `stat(28,code)` (raw scancode held), `stat(120)`/`stat(121)` (stdin/serial data pending) implemented with their oracle-confirmed types: booleans (false) and empty string.** The generic numeric-0 fallback was TRUTHY in Lua, so `while stat(30) do ... end` key-drain loops spun forever. `terra_1cart-42.p8.png` gets past those loops now but still hangs inside a map-generation `repeat ... until mget(...)` loop in `_init` — a deeper divergence not yet located. | `src/pico_lua/api.rs` (`api_stat`)

Corpus re-sweep: **178/188 clean** (unchanged) — **zero regressions**.

### Follow-up: pushing toward 100% clean — big-map registers done right (round 32)

Located `terra_1cart-42.p8.png`'s `_init` hang with a new opt-in `PICOR_TRACE=1` statement-count/line tracer: an infinite crystal-placement loop sampling map cells at x∈0..255, y∈60..117 — coordinates that only exist in PICO-8's big-map mode (`poke(0x5f56,0x80,0)`: map base page 0x80 → 32KB of upper RAM as a 256-wide, 128-tall map).

- **`mget`/`mset` with a custom map base now address the whole custom region row-major — including x<128 — with height = however many full rows fit between the base and end of RAM.** The old code kept x<128 on the classic 0x2000 accessors regardless of the base register and capped y at 64. All semantics oracle-confirmed with a dedicated probe (writes to the region land at base*256 + y*width + x; the classic map is untouched while a custom base is active and intact after restoring it; y=128 is OOB for the 0x80 base). | `src/gfx.rs` (`custom_map_region`, `map_get_wide`, `map_set_wide`)
- **The map base register 0x5F56 was never initialized to its default 0x20** — with the new custom-region path, a fresh cart's mget/mset would have redirected into the sprite sheet. Now set alongside the width register's 128 default in both `Memory::new` and `init_draw_state`. | `src/memory.rs`
- Also added the `PICOR_TRACE` hang locator itself (per-20M-statement line report, env-gated, zero cost when off). | `src/pico_lua/interp.rs`

Corpus re-sweep: **179/188 clean** (up from 178) — **zero regressions**. One full clean fix (`terra_1cart-42.p8.png`).

### Follow-up: pushing toward 100% clean — closures captured stale shadowed bindings (round 33, foundational)

Located via the new key-carrying index-error message (`attempt to index a number value (key "h")` pointed straight at an object whose constructor-captured closure held a number).

- **A closure now captures the NEWEST binding of a shadowed local name — the old capture pushed duplicate names in declaration order and the upvalue lookup took the FIRST match, binding the stale outer local.** The golfed OOP idiom `function f(n) local n={...} n.B=function() return nB(n) end return n end` — a constructor whose object shadows its own parameter — captured the parameter (a number) instead of the object table. Fixed by scanning locals innermost-first with dedup at capture time. Oracle-verified (`probes/closure_shadowed_capture.p8`). | `src/pico_lua/interp.rs` (`make_closure`)
- Also: index errors now name the key (`(key "h")`), matching the diagnostic spirit of official's "attempt to index field 'h'" and making this class of bug findable in seconds. | `src/pico_lua/interp.rs` (`table_get`)

Corpus re-sweep: **182/188 clean** (up from 179) — **zero regressions**. Three full clean fixes: `driftmania-5.p8.png`, and — previously misattributed to the deferred fixed-point epic — `picodex_dual-1.p8.png` and `super_world_of_goo-6.p8.png`, whose decompressor failures were this same stale-capture bug all along.

### Follow-up: pushing toward 100% clean — constructor nil-span for `#`/unpack (round 34)

- **A table constructor's positional span now counts nil slots, and the border search consults it the way luaH_getn consults Lua's array-part size.** `{...}` from varargs `(nil,nil,49,nil,1,nil,nil,"x")` places every value at its correct index but left `t[1]` empty, so the plain border search reported `#t == 0` and `unpack()` returned nothing — official reports 8 (the constructor pre-sizes the array part). `TableInner` now records the constructor-declared span (`array_decl`); the border search starts there (extending upward for appended elements, or binary-searching within the span when its last slot is nil). Oracle-verified (`probes/vararg_nil_span.p8`). This was the real cause of `fakogejuzo-0.p8.png`'s "compare nil with number": its `new_type` class helper builds objects as `container(nil,nil,49,nil,1,...)` and destructures with `unpack(self)`. | `src/pico_lua/value.rs` (`array_decl`, `raw_len`), `src/pico_lua/interp.rs` (`eval_table`)

Corpus re-sweep: **183/188 clean** (up from 182) — **zero regressions**. One full clean fix (`fakogejuzo-0.p8.png`).

### Status of the last five non-clean carts (post-round-34)

- `basicshmupdc1-0.p8.png` — **conformant-by-matching-error**: unterminated `--[[` fails identically on official hardware. Strict-exit-code max is therefore 187/188.
- `praxis_fighter_x-2.p8.png`, `redash-7.p8.png` — both fail inside cart-embedded LZW-style decompressors whose bit arithmetic relies on 16.16 overflow behavior (`2^n` for n≥15 saturates at 32767.99998 on real hardware and feeds `%`-masks; full-precision f64 gives entirely different values). **Blocked on the exact-fixed-point epic running in the parallel session.**
- `samurise-1.p8.png` — infinite loop inside its embedded `parens8` LISP VM (`_update60`, cart line 141): ruled out slowness with a 280-second run (~80M statements, single line, no progress). The specific interpreter-semantics divergence feeding the VM's dispatch loop is not yet located.
- `bytebeat_tweet-0.p8.png` — paces its generator loop on `stat(108)` (serial audio queue) *draining in real time as audio plays*; a headless fixed-frame run has no analogue for that. The queue counter itself is implemented and oracle-locked.

### Follow-up: pushing toward 100% clean — all() iterates strings (round 35)

Located `samurise-1.p8.png`'s infinite loop with the upgraded PICOR_TRACE (statement-kind + call-depth): a single `_ppos+=1` assignment spinning inside its embedded `parens8` LISP VM's scanner, `while(function()for e in all(e)do ... end end)()==n do _ppos+=1 end`, where `e` is the STRING `" \n\t"`.

- **`all()` over a string iterates its characters as 1-char strings — implemented, oracle-confirmed** (`for c in all("abc")` yields a,b,c on real hardware; ours returned an empty iterator, so the scanner's char-set test never matched and `nil==nil` kept the advance loop alive forever). | `src/pico_lua/api.rs` (`api_all`)

Corpus re-sweep: **183/188 clean** (unchanged) — **zero regressions**; `samurise-1.p8.png` progressed from the infinite TIMEOUT to a locatable LOAD_ERROR deeper in the VM's compile stage (`attempt to index a nil value (key 2)`), still under investigation.

### Follow-up: pushing toward 100% clean — insertion-ordered tables, readrom (round 36)

- **`pairs()`/`next()` iteration is now insertion-ordered and stable (the long-standing "HashMap ordering" LEDGER item).** samurise's embedded LISP VM failed with a DIFFERENT error on every run — the definitive symptom of iteration-order dependence — and is now fully deterministic. `TableInner.map` is a new `OrderedMap` (HashMap index into an insertion-ordered entry vec with tombstone compaction) exposing the same surface, so all 31 call sites compiled unchanged. | `src/pico_lua/value.rs`
- **`readrom(addr, len)` implemented to documented semantics** (returns ROM bytes as a string). NOT oracle-lockable: the 0.2.7 official binary reports `readrom` as nil under `-x` even though it's documented and works interactively — noted in code. samurise's VM boots its LISP stdlib with `(parens8 (readrom 0x2000 0x0fa3))`. | `src/pico_lua/api.rs`
- Top-level (load-stage) runtime errors now carry `cart:<line>` like the lifecycle-stage errors. | `src/pico_lua/mod.rs`

Corpus re-sweep: **183/188 clean** (unchanged) — **zero regressions**. samurise now fails deterministically (`cart:141: attempt to index a nil value (key 2)`) inside its VM's compile stage — still open, but debuggable now.

### Follow-up: pushing toward 100% clean — object keys round-trip through next() (round 37)

Peeled another layer off samurise's parens8 VM via a shrinking-differential campaign (probe carts embedding the VM's real boot chunks, bisected against the oracle):

- **`next()`/`pairs()` now round-trip table- and function-valued KEYS — the old `Key` registry stored only a raw pointer, so iteration returned nil for object keys and ENDED EARLY, silently truncating any table keyed by objects.** samurise's VM keys its per-closure upvalue sets by sentinel TABLES and copies them with `for e in next,n do t[e]=d[1][e] end` — the truncation broke its captured-environment chains, producing the deterministic `(key 2)` failure. `Key::Table`/`Key::Function` now hold the real `Rc`/`Function` (hash/eq still by identity), and `key_to_value` reconstructs them. Oracle-verified (`probes/next_object_keys.p8`). | `src/pico_lua/value.rs`, `src/pico_lua/api.rs`
- The Lua call-depth cap is now a host-settable `recursion_limit` (default 1000), and run-cart executes on a 1GB-stack worker thread with the cap at 30k — deeply recursive embedded compilers need real depth. | `src/pico_lua/interp.rs`, `src/pico_lua/mod.rs`, `tools/run_cart.rs`

Corpus re-sweep: **183/188 clean** (unchanged) — **zero regressions**. samurise's VM now compiles further still but hits runaway recursion inside its compile stage ("out of memory" even at 30k frames — some remaining semantic divergence turns its compile into unbounded recursion; each differential probe so far has confirmed the VM machinery piece by piece).

### Follow-up: pushing toward 100% clean — count() on non-tables; samurise clean (round 38)

The final layer of the samurise onion, located by extracting its ROM-embedded LISP stdlib from the cart PNG, bisecting it form-by-form on a mini-boot cart, and minimizing the failing form to a two-token repro:

- **`count()` on a non-table returns NO values (like `tostr()` with no args), not 0 — fixed, byte-exact against the oracle.** 0 is truthy in Lua, and samurise's LISP VM dispatches key-value vs positional table entries with `(when (count elem) kv-branch positional-branch)` — our truthy 0 sent every positional ATOM down the kv branch, whose `(rawget elem 2)` on a string produced the unbounded compile recursion behind the "out of memory". Minimal repro: `(table 8 5 6)`. | `src/pico_lua/api.rs` (`api_count`)

**`samurise-1.p8.png` now runs its full frame budget clean** — the end of a five-layer dig across rounds 35-38, each layer a real, probe-locked conformance fix: glyph aliasing → all()-over-strings → object keys through next() → recursion-limit hosting → count() arity. Corpus re-sweep: **184/188 clean** (up from 183) — **zero regressions**.

**Remaining 4:** `basicshmupdc1-0` (broken identically on official — conformant-by-matching-error; effective score 185/188), `bytebeat_tweet-0` (paces on real-time audio drain; no headless analogue), `praxis_fighter_x-2` + `redash-7` (cart-embedded decompressors requiring exact 16.16 fixed-point overflow semantics — the epic in progress in the parallel session).
